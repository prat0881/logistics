# Stage 5 — Session Handoff

_Last updated: 2026-08-22 (S5.6 + S5.7 + S5.8 + S5.9 + **S5.9.1 all COMPLETE**. **All open points are consolidated in the register below** — start there.)_

## Current stage & branch
- **Stage 5 — Compare Quotes & Award.** Branch `feat/stage-5-fx-master` → **PR #52** (OPEN, **not merged** — you merge manually).
- S5.9 (Approval Flow & Compare Screen Rework, 10 tasks) landed on top of S5.4–S5.8: `docs(s5.9): design of record + implementation plan` (`13625c7`) through Task 10's tooltip move + its handoff. Code tip through Task 9: `b3834e5`; whole-branch review fix wave `ef470e4..20dfba9`.
- **S5.9.1** (Compare Screen Review Fixes, 5 tasks — nine product-owner review points against the delivered S5.9 screen, plus one live-discovered hardening task) landed on top: `docs(s5.9.1): implementation plan …` (`640ab33`) through Task 4's notification + this handoff (see its own section below).
- `pnpm run ci` **GREEN**: shared 26 files/390 tests · web 115 files/832 tests · api 93 suites/445 tests · lint + typecheck + 3 builds clean. No flakes hit in Task 4's full-gate run (register **C5**'s three known flakes — `award-generate.e2e-spec.ts`, `ff-portal-v3.e2e-spec.ts`, `charge-catalogue.e2e-spec.ts` — all green this run).

## What's built (9 sub-builds, all on PR #52)
| SB | Scope | State |
| :-- | :-- | :-- |
| S5.1 | FX master (`FxRate` + `toUsd` + `fx-rates` module + `masters/fx-rates` screen) | ✅ merged into PR |
| S5.2 | Comparison engine + read model (`recommendOffer`, `GET …/comparison`) | ✅ |
| S5.3 | Status & decision foundations (enums, machine edges, `LegAwardDecision`/`AwardDecisionEvent`, `awardSnapshot`) | ✅ |
| **S5.4** | **Approval workflow endpoints** — shortlist / send-for-approval / approve / reject / generate-client-quote / reopen-comparison | ✅ delivered this session |
| **S5.5** | **Negotiation §10.1 + Change-order reversal §10.2** | ✅ delivered this session |
| **S5.6** | **Compare-Quotes frontend** (the screen per the mockup + FX admin) | ✅ COMPLETE — all 6 tasks, reviewed + final-review-fixed, ci green, visually verified, **pushed to PR #52** (`7b58a8d..354236e`) |
| **S5.7** | **Compare-Quotes UI/UX enhancements** (9 items, frontend-only) | ✅ COMPLETE — 6 tasks + final review + visual acceptance (`6c5f5d1..5f5c7bf`) |
| **S5.8** | **Client quotation** — margin, editable charges, preview, issue | ✅ COMPLETE — 6 tasks + final review + fix wave (`05d137b..b54a3ef`), ci green |
| **S5.9** | **Approval Flow & Compare Screen Rework** — `PENDING_APPROVAL` leg/quote statuses, transactional send-for-approval, approve/reject drive status, portal fixes, compare-screen rework | ✅ COMPLETE — 10 tasks, per-task review + fix loops, ci green (see its own section below) |
| **S5.9.1** | **Compare Screen Review Fixes** — recommendation marker survives a send, checker action-bar confirmations, Negotiate hidden for Manager/Admin, dense aligned columnar grid, shortlisted-offer marker, reject notifies Executives | ✅ COMPLETE — 5 tasks, per-task review, ci green (see its own section below) |

Every sub-build was built subagent-driven (TDD, per-task review + fix loops, **opus whole-branch review**). S5.4's reviews caught 5 real defects before merge; S5.5's caught the `StatusRegistry` init-order issue + the request-requote/`QUOTING_CLIENT` teardown seam. S5.9's caught the `REQUOTED` portal dead-end (see its own section — found and fixed, no frontend coverage existed for it). All findings fixed or adjudicated.

## Decisions made (why) — **please review**
- **S5.4 RBAC — generate = Manager+ ONLY, no four-eyes (your O4 call).** The design self-contradicted (§4/§11/§13 tables said four-eyes on generate; §16 O4 said no). You chose **O4**. Design doc §4/§11/§13/D9/§12/§15 all aligned to it. approve/reject keep four-eyes; generate is Manager+ only.
- **S5.5 D-A — single-leg re-quote resets the whole (query×FF) RFQ deadline.** `Rfq` is `@@unique([queryId, freightForwarderId])` (one RFQ covers all of an FF's legs on a query; no per-quote deadline). §10.1 says "reset that RFQ's deadline", so a re-quote on one leg extends the shared window for that FF's other open legs too. Accepted per the design's RFQ-level model.
- **S5.5 D-B — modified the delivered SB6 change-order subsystem** to cascade `APPROVED` quotes (`scope.resolver.ts` + `change-order.strategy.ts`). Required for §10.2 to fire at all (an approved leg was invisible to change-order before). Low blast radius (APPROVED post-dates SB6); full SB6 regression green.
- **S5.5 D-C — new `rfq.requote_requested` notification template** (rather than overloading `rfq.updated`, whose copy is "a leg was added").

## ⚠️ Vocabulary — read before touching any user-facing string

**Nothing in Stage 5 is *awarded*.** Design decision **D5** locks this: *"'Approved' is provisional; term 'Awarded' is reserved for Stage 6+"* — approval selects a forwarder for a leg **with no forwarder notification**, and stays reversible until the client wins. At the end of Stage 5: no forwarder has been told anything, and the client has not accepted.

| Say this | Not this | Because |
| :-- | :-- | :-- |
| **Approved** (a leg, provisionally) | Awarded | No FF has been notified; still reversible |
| **Quotation** (the stage, the screen) | Award | The step produces and issues a client quotation |
| **Issue** a quotation | Send | `LogTransport.send()` is a no-op — nothing is transmitted yet |
| **Quoting client / Awaiting client decision** | Won | The client hasn't accepted |

`LegStatus.AWARDED` exists in the enum but is **deliberately unreachable** — reserved for the post-Won execution phase. Internal backend names (`awardSnapshot`, `LegAwardDecision`, `award.service.ts`, `AwardModule`) keep "award" and that is fine; the design itself uses them. **The rule applies to user-visible strings.**

*This was caught by the product owner on 2026-08-19: the S5.8 stage rail said "Award" and linked to the quotation screen. Fixed in `ac424f1` (label and key are now "Quotation"). Recording it here so a fresh session doesn't reintroduce it.*

## 📋 Open points register — consolidated 2026-08-19, updated 2026-08-22 (S5.9)

Everything still open across S5.4–S5.9, in one place so nothing is lost between sessions. Nothing here blocks merging PR #52.

### A · Business decisions — these need your answer, not engineering
| # | Question | Why it's open | Raised |
| :-- | :-- | :-- | :-- |
| **A1** | **What is a quotation "valid until"?** Currently the **earliest `validUntil` across the winning quotes** — never promising the client longer than the forwarders promised us. | No such field exists on the query or the award. The final review ruled the current rule correct, but it is the one commercial assumption made on your behalf. | S5.8 Q2 |
| **A2** | **How should charges be grouped for the client-facing breakdown?** | S5.7's charge dialog shows three rolled-up subtotals because the itemised lines never leave the server. Exposing them is easy; **which grouping** is a business call. Blocks B5. | S5.7 C2 |
| **A3** | **Is Reopen meant to be actionable?** `reopenComparison` deliberately leaves every decision `APPROVED`, so after a reopen there is **no path to revise a shortlist** — reject 409s and the reopen already happened. | Either Reopen should also reset decisions to `DRAFT`, or the UI should say plainly that it only unfreezes the snapshot. The misleading copy was fixed; the flow question stands. | S5.6 |
| **A4** | **Should the earlier price survive a re-quote that expires?** §10.1 promises the FF's earlier price "stays visible, badged stale", but the expiry sweep clears `draftJson`, so if the FF ignores the request the original offer is **lost** with no fallback. | Intended business rule, or a gap? | S5.5 |
| **A5** | **Should query status surface approval progress?** Shortlist → send → approve → every leg approved all happen **inside `QUOTED`** without the status changing once. | A query at `QUOTED` may have nothing shortlisted or be one click from a client quote. Cheapest fix isn't a new status — it's a "2 of 3 legs approved" indicator, and the data is already in the read model. **Still open by choice after S5.9**: the whole approval flow (`PENDING_APPROVAL` leg/quote statuses, the maker/checker screens) was built and rebuilt this sub-build without touching query-level status — a query reads plain "Quoted" throughout send/approve/reject exactly as before. | Status review |

### B · Backend follow-ups — bundle these into ONE API task
All are API-shape changes on the same feature. **B1, B2 and B3 were closed by S5.9** (see section D below for how); what's left needs its own trip.

| # | Gap | Fix |
| :-- | :-- | :-- |
| **B4** | **Multi-forwarder negotiate is not atomic** — N sequential calls, no transaction. A mid-batch failure leaves some forwarders asked and others not, with tokens already reissued and deadlines reset for the successes. | A bulk endpoint taking `{quoteIds[], notes}` in one transaction. |
| **B5** | **Itemised charges never leave the server** for the compare screen — `computeQuoteTotals` collapses them to three sums. *(S5.8 built its own server-side read path; this is the compare-screen half.)* | A grouping-neutral passthrough of the raw lines. **Blocked on A2.** |
| **B6** | **Wire `REQUOTED` into the change-order cascade.** A field edit on a leg whose only quote is `REQUOTED` free-paths, so the FF could re-quote against a stale basis. Reviewed as safe to defer. | New `REQUOTED→INVALIDATE→INVALID` edge. Background task `task_44674c1e`. |

### C · Known gaps and tech debt
| # | Item |
| :-- | :-- |
| **C1** | **Nothing is actually emailed.** `LogTransport.send()` is a no-op until `SmtpTransport` lands at the go-live gate. Issuing composes, records and advances status; no email reaches the client. Already a planned go-live item. |
| **C2** | **The quotation email subject is caller-supplied free text.** The body is safe by construction (one shared render call site; no path to cost, margin or forwarder names). The subject is the only opening — a deliberate envelope-editable decision that survives the SMTP gate. |
| **C3** | **Pre-existing Prisma drift.** Five earlier migrations hand-wrote `DEFAULT gen_random_uuid()` in raw SQL while `schema.prisma` uses client-side `@default(uuid())`, so every future `migrate dev` proposes 10 unrelated `ALTER COLUMN "id" DROP DEFAULT` statements. **Verified unchanged by S5.8, and all 33 migrations apply cleanly to a fresh DB.** Worth one dedicated cleanup migration. |
| **C4** | **Eight enum values are unreachable** — `WON`/`LOST`/`CLOSED`/`AWAITING_CLIENT_DECISION` were declared on the query (S5.8 now reaches the last of these); `AWARDED`/`IN_TRANSIT`/`DELIVERED`/`CLOSED` on the leg; `CLOSED` on the quote. Plus `AwardDecisionStatus.REJECTED`, which nothing writes (reject writes `DRAFT` + a reason). Stage 6–9 vocabulary declared early — harmless, but it makes the status picture look twice its real size. |
| **C5** | **The api e2e suite has an intermittent cross-test flake.** `award-generate.e2e-spec.ts` ("combinedUsd is re-rounded", float summation order), `ff-portal-v3.e2e-spec.ts`, and (newly observed during the S5.9 fix wave) `charge-catalogue.e2e-spec.ts`'s "rejects unauthenticated requests" have each failed once in a full `--runInBand` run and passed on every re-run and in isolation. Shared-DB / socket-timing artifacts of the full serial run, not defects. Hosted CI could hit any of the three. The `charge-catalogue` instance failed once with `Parse Error: Expected HTTP/, RTSP/ or ICE/` — a Node HTTP-parser/socket-level artifact, not an assertion failure — then passed 3/3 in isolation and passed in a subsequent independent full `--runInBand` gate; nothing in the fix wave touches that file or the endpoint it exercises (`GET /api/charge-line-definitions`, an auth-only 401 check). Two independent agents confirmed it is a flake, not a regression. **Watched, not reproduced since the one observed instance.** |
| **C6** | **S5.8 has no visual pass.** Builder and preview are test-covered only. Worth eyeballing before it goes near a client. S5.6 and S5.7 were both verified live. |
| **C7** | **A re-quote is invisible at query level.** Asking a forwarder to revise a price on an already-`FULLY_QUOTED` leg moves neither the leg nor the query — there is no backward edge and the projector excludes re-quotes from "settled". The compare screen shows it; no status does. Related: a `REQUOTED` quote expiring on a `FULLY_QUOTED` leg fires no transition, so the query can read `QUOTED` while that leg holds only an `EXPIRED` quote. |
| **C8** | **NEW (S5.9) — a live forwarder portal link is readable by any authenticated user, not just Managers/Admins. Severity RAISED in the final whole-branch review; PO ruling: stays open here, NOT fixed in this branch.** `GET /api/queries/:id/emails` returns `bodyRendered` — the rendered `rfq.invitation` email, including the working `{{Access_Link}}` — with **no `@Roles`** on the route, for **any** query, not just ones the caller works on. **The impact was understated as "readable".** That link is an *unauthenticated bearer credential* for the `@Public` forwarder portal (`/ff/rfq/:token`, `RfqTokenGuard`): possession is the entire authentication. So any authenticated **Executive** can open any query's forwarder link, read that forwarder's frozen cargo manifest, and **submit a quote as that forwarder** — impersonation and manifest disclosure, not just link disclosure. It compounds with **D7**, which removed the only mechanism that ever aged such a link out: the token no longer rotates on re-fetch and its raw value is persisted (`Rfq.accessToken`), so a link read off this endpoint stays live indefinitely. **D8 does not widen this route**: `rfq.requote_requested` is written with `entityType: "RFQ"`, and this listing filters on `entityType: "QUERY"`, so re-quote emails do not surface here at all — the exposure is exactly the original `rfq.invitation` rows. `Rfq.accessToken` itself was re-verified as read in exactly one place (`negotiation.service.ts`) and reaching no DTO, response or log — the exposure is entirely through `bodyRendered`. **This predates S5.9 entirely** and is deliberately not fixed in this branch (PO ruling: own register item, own trip). The fix stays small (`@Roles` on the emails route, or stop echoing `bodyRendered` to non-privileged callers) — but it should be prioritised as an impersonation gap, not a link-hygiene one. |

### D · Reviewed and sound — informational only
- **Terminology (D5) — corrected 2026-08-19.** The S5.8 stage rail labelled the quotation step "Award", violating a locked decision. Renamed label *and* internal key to "Quotation" (`ac424f1`); `LegStatus.AWARDED` and the backend's internal `award*` names deliberately untouched. See the Vocabulary section above.
- **`StatusRegistry.contribute()` is now module-init-order-independent** (`pending`-merge + an `OnApplicationBootstrap` orphan-key guard). Touches the Extensibility Core used by every domain module; the merge was proven correct for all orderings against the actual `@nestjs/core` scanner source. Flagged only because it is shared infra.
- **The two status reference views** built during the status alignment discussion — a technical one (four axes, what can coexist beneath a query status) and a business one (the full lifecycle, one line per transition) — are Claude artifacts, not in the repo. Regenerate from the code if needed; both were derived from source, not the design docs.
- **B1, B2 and B3 — closed by S5.9.**
  - **B1** (negotiate block at `PENDING_APPROVAL` was UI-only): closed in Task 5 by an explicit **leg-level** guard in `negotiation.service.ts` — before anything else, it loads the leg's `LegAwardDecision` and 409s if that decision is `PENDING_APPROVAL`. **CORRECTED (final whole-branch review, IMPORTANT 2).** This note previously said `REQUOTABLE_STATUSES = [QUOTED, APPROVED]` "didn't need to change, because the shortlisted quote itself now genuinely leaves `QUOTED`" — reasoning the code beside that guard *explicitly rejects*. `REQUOTABLE_STATUSES` gates only the quote NAMED in the call, so a still-`QUOTED` **sibling** quote on the same leg sails straight past it and resets a decision a checker is mid-review of on a *different*, already-shortlisted quote. The quote-status route is therefore **not** sufficient and the leg-level guard is **not** redundant — do not delete it on the strength of the old wording. PO ruling **D5** confirmed the intended behaviour (negotiate stays refused while pending approval) and the UI (`NegotiateDialog`'s eligibility list, `CompareLegPanel`'s disabled-reason text) now matches the server exactly instead of contradicting it.
  - **B2** (`sendForApproval` had no decision-status guard): closed in Task 3. The send path now runs in one transaction that takes a row lock (`SELECT … FOR UPDATE`) on the `Leg` before checking the quote's status, so two concurrent sends on the same leg can no longer both succeed, and an already-`PENDING_APPROVAL`/`APPROVED` quote 409s a re-send. The reviewer reproduced the original double-send 4/4 before the fix and 0/4 after.
  - **B3** (`send-for-approval` carried no offer identity): closed in Tasks 3 and 9 together. The endpoint now accepts the offer's `{quoteId, variant}` directly and verifies it server-side (`COMPARABLE_STATUSES`-gated) rather than trusting the persisted shortlist; `SendForApprovalDialog` (Task 9) is the one caller and always names the offer it sends. `ShortlistDialog` and its separate persisted-shortlist step are retired.
- **D7/D8 (S5.9 T5) — reviewed sound in isolation.** The portal token stopping rotation and the raw value being persisted (`Rfq.accessToken`) was reviewed against an adversarial payload set and found not to widen exposure: the column reaches no DTO, response or log anywhere in the codebase. See register **C8** above for why this is still worth your attention — a *different*, pre-existing endpoint already leaks the same working link to a wider audience than intended, and D7/D8 is the reason that leak, once read, now stays live indefinitely instead of rotating out.

## Approaches that didn't work
- **S5.5 module-init ordering:** wiring `AwardModule → RfqModule` shifted NestJS's computed module-init "distance", flipping `RfqModule.contribute("leg")` ahead of `LegsModule.register(legMachine)`. Import-graph tweaks (e.g. `RfqModule` importing `LegsModule`) do **not** fix it (single-pass non-fixed-point DFS). Root-fixed in `StatusRegistry` instead (see item 4).
- **S5.4 generate winner-pricing:** cannot reuse `getComparison` for winners — `COMPARABLE_STATUSES` excludes `APPROVED`, so approved winners vanish from the grid. Generate prices winners directly from each winning quote's immutable `draftJson`. *(Corrected in the final whole-branch review: this line used to quote `COMPARABLE_STATUSES = [QUOTED, REQUOTED]`, stale since S5.9 Task 2 added `PENDING_APPROVAL` to that set. The exclusion that matters here — `APPROVED` — is unchanged, so the conclusion stands.)*

## S5.6 (Compare-Quotes frontend) — ✅ COMPLETE (6 of 6 tasks)
Plan: `docs/plans/stage-5/Stage 5 - S5.6 - Compare Quotes Frontend - Implementation Plan.md`. SDD ledger (full per-task detail + reviews): `.superpowers/sdd/Stage 5 - S5.6 …/progress.md`. Frontend-seam research: `scratchpad/s5.6-frontend-research.md`. Approved visual spec = mockup artifact `10b4cbc3-c713-4773-b8c5-00ea8289cec4` + design §12.

**⚠ Scope correction:** S5.6 is NOT pure-frontend as §15 implied — `GET …/comparison` didn't return the award decision / timeline / itemised charges the maker-checker UI needs. **Task 1 added them** (shared `AwardDecisionDto`/`AwardDecisionEventDto`/`OfferChargeLineDto` + `ComparisonService` join; NO DB migration — tables pre-existed).

**Done — ALL 6 TASKS, all reviewed. Commits `7b58a8d..354236e` (13 commits), pushed to PR #52:**
- **T1** `c87201f` — backend read-model extension (decision + timeline + itemised charges on `GET …/comparison`).
- **T2** `99ca41f`/`d0e4d52` — route `/queries/:id/compare` + `CompareQuotesPage` shell (reuses `QueryOverviewHeader` + `RouteDiagram` w/ a new `selectedLegId`/`onSelectLeg` channel + a single-open `CompareLegPanel`) + `StageRail` "Quotes" step wiring (`isQuotesStageEnabled` at `RFQ_SENT`+).
- **T3** `0b65db4`/`6c4c621` — read-only `ComparisonGrid` (`(FF×variant)` columns) + `RecommendationBanner` + click-FF `OfferDetail` + pending/awaiting. Unpriced offers greyed, never a fake `$0`.
- **T4** `58390ae`/`b2d12b3` — **maker controls**: shortlist radios (override-reason when ≠ recommendation) + send-for-approval (A9 proceed-without-waiting) + per-FF Negotiate dialog. Hooks in `useAwardActions.ts`.
- **T5** `a85bd80`/`93987f3` — **checker view**: `CheckerPanel` (Manager+, four-eyes-disabled when `sentByUserId === user.id`) + `GenerateGate` (Manager+, NO four-eyes per O4) + `DecisionTimeline` (all modes) + `useApprove`/`useReject`/`useGenerateClientQuote`.
- **T6** `0b516c8`/`513d22e` — **Quoting-Client end state**: `QuotingClientPanel` (per-leg winners + `combinedUsd` + FX-as-of + Reopen) + `useReopenComparison` + a single `locked` boolean that **unmounts** (not merely disables) Maker/Checker/Generate.
- **Final-review fix wave** `354236e` — see below.
- **`pnpm run ci` GREEN** at `354236e`: shared 366 · web 105 files/665 · api 90 suites/382 · lint + typecheck + 3 builds.

**⚠ Second scope correction (T6):** `Query.awardSnapshot` was unreachable from the frontend (neither `ComparisonDto` nor `QueryDetail` exposed it). Resolved by adding `awardSnapshot` — and later `forwarderNames` — to **`ComparisonDto`** (feature-scoped) rather than `QueryDetail` (wide, app-wide). No migration. The persisted `QueryAwardSnapshotLeg` type was deliberately left untouched so a read-time-only field never masquerades as stored.

### Final whole-sub-build review (opus): 1 Critical + 2 Important + 3 Minor — ALL fixed in `354236e`, re-reviewed clean
The per-task reviews were task-scoped; the broad review caught what they structurally could not:
- **C1 (Critical) — the T3×T4 seam.** `SendForApprovalSection` couldn't see the current pick, while a grid-header click silently re-points the shortlist radio. So: inspect another FF's charges → press Send without pressing Shortlist → **the server sends the previously-persisted offer, returns 200, and the checker approves the wrong forwarder, with no error anywhere.** Fixed with an `unsavedPick` guard (shared `offerKey` on both sides) enforced in `onSend` **and** `disabled`.
- **I1** — after a Reopen, `MakerPanel` told users to "reject or reopen it", both impossible for an `APPROVED` decision. Copy corrected (the underlying product question is open item 1 below).
- **I2** — nothing writes `REJECTED` (`reject()` writes `DRAFT` + `rejectionReason`), so a rejected leg's card read "Shortlisted" and the reason rendered nowhere in the app. Now an inline alert + a "Rejected — revise" chip; dead branches removed.
- **M1** — post-award the grid re-ranked the *losers* and displayed "Recommended: \<losing FF\>" directly above the award panel. `locked` now suppresses the badge + banner.
- **M2** stale REQUOTED offers were unlabelled in the shortlist radio; **M3** a latently-vacuous locked-state assertion.

### Visual verification — DONE (live dev server + seeded data)
Seed script (scratchpad, **not** in the repo; idempotent, `--cleanup`): `scratchpad/seed-compare-quotes.ts` → query **S56VIS-0001** `83d5770f-959b-4ccd-b18c-d353ee9ad2d9` (2 legs ROAD+SEA, 4 FFs, INR/AED/GBP + matching FX rows, no USD). Logins `exec@svyft.local`/`exec-dev-password`, `manager@svyft.local`/`manager-dev-password`.
Confirmed live: the full grid (6 `(FF×variant)` columns, USD+native, unpriced "—" never `$0`, REQUOTED stale badge, recommendation ring, pending/awaiting) in **light and dark**; the whole maker flow including **C1's guard releasing exactly when the shortlist persists**, and A9's proceed-without-waiting + reason; **RBAC live** (Executive sees no checker panel and no Generate gate; Manager sees both); the checker panel + `GenerateGate` disabled with "0 of 2 legs approved."; and inline `role="alert"` error surfacing proven against two real server guards.
**Not verified live: `QuotingClientPanel` + Reopen** — reaching it needs every leg `FULLY_QUOTED`, which requires SB5's expiry sweep; `award.service.ts:246-253` warns that hand-firing a bare EXPIRE strands SB5's `ScheduledEvents`, so it was not fabricated. Covered by unit tests + e2e.
**Note (pre-existing, app-wide, not S5.6):** dark mode is unreachable in the running app — `darkMode:["class"]` + `.dark` tokens exist, but nothing ever adds the class; there is no theme toggle.

**Parked minors (rulings in the S5.6 ledger; the final review triaged ALL as defer):** T1 M1 (1¢ per-line vs total rounding — `OfferDetail` shows the authoritative `usdTotal`, test-pinned), T2 M2 (`RANK.QUOTING_CLIENT=4` is spec-literal — do not key a new threshold off it), T3 #4/#5/#6 (REQUOTED wording / `money.ts` DRY / aria-controls), T4 M3 (RHF override text persists across picks — harmless now C1's guard exists), T5 M3 (CheckerPanel-self-gates vs GenerateGate-parent-gates — independently re-confirmed correct), T6 (locking-test file placement). The FX-admin screen (§12) **already exists** from S5.1.

**Pushed to PR #52 on 2026-08-17** (fast-forward `fb4935b..33cc0b5`); PR title + body updated to cover S5.6; CI green. Open items 5 and 6 above remain for you.

## S5.7 (Compare-Quotes UI/UX enhancements) — ✅ COMPLETE
Nine enhancements against the delivered S5.6 screen. Built subagent-driven (6 tasks, TDD, per-task review + fix loops, **opus whole-sub-build review**), then visually accepted live. Commits `6c5f5d1..5f5c7bf` (11).
- **Design of record:** `docs/Stage 5 - Compare Quotes UI Enhancements - Design.md`
- **Implementation plan (SDD, 6 tasks):** `docs/plans/stage-5/Stage 5 - S5.7 - Compare Quotes UI Enhancements - Implementation Plan.md`
- **Mockups** (kept, git-ignored): `.superpowers/brainstorm/62958-*/content/` — grid orientations, charge groupings, page structures, dialogs.

**🔴 Hard constraint: FRONTEND ONLY** (requester's call). No api / `packages/shared` / prisma changes.

| # | Item | Decision |
| :-- | :-- | :-- |
| 1 | Tabular grid, FF's variants grouped | **Both orientations, user-switchable** (`localStorage`), explicit FF separator |
| 2 | Charge breakdown parent→child pop-up | UI-only against the 3 existing lines; component built tree-shaped for later |
| 3 | Recommended by colour | Tint + badge; **`RecommendationBanner` deleted**, reason moves to a tooltip |
| 4 | Shortlist in-grid → send-for-approval pop-up | **Merged into ONE dialog** |
| 5 | No Negotiate once sent for approval | Disabled with a visible reason |
| 6 | Leg-level multi-FF Negotiate | Shared note + optional per-FF notes; eligibility per FF |
| 7 | Less scrolling | **No structural change** — legs stay stacked, one open; diagram-click already works (T2) |
| 8 | Exec view aligned | Same screen, both roles tested explicitly |
| 9 | Conversion rate shown | `OfferDto.unitsPerUsd` — already in the payload |

**Notable:** item 4's merge **structurally eliminates** the Critical the S5.6 whole-sub-build review caught (picked offer and persisted shortlist could drift). The `unsavedPick` guard added in `354236e` becomes unreachable and is removed — **its regression tests are ported, not deleted** (plan Task 4 Step 5).

### 🔶 Consequences of the frontend-only constraint — now tracked as B1–B5 in the register
Three real gaps, accepted deliberately. Full detail in the design doc's "Consequences" section.
1. **C1 — item 5 is a UI convention, not a rule.** `AwardService.requestRequote` still accepts a re-quote on a `PENDING_APPROVAL` leg and resets the decision to `DRAFT`. The button is disabled; the endpoint is not. *Follow-up:* add a decision-status guard (409), or accept the reset and drop the UI restriction — the two currently contradict each other.
2. **C2 — item 2 shows subtotals, not a breakdown.** `buildCharges` emits only 3 flat aggregates (`Freight`, `Additional Charges` = ONE sum over all origin/destination/ad-hoc lines, `Warehousing` = ONE sum). The itemised detail exists in each quote's `draftJson` (`charges[]` w/ zone+label+amount+note, `trucking[]`, `seaRates[]`, `warehouse[]`) but `computeQuoteTotals` returns only sums. *Follow-up:* a grouping-neutral read-model passthrough of the raw lines, letting the UI group them once business confirms. Business has NOT yet confirmed the grouping — that's why this is deferred.
3. **C4 (NEW, from T4) — `send-for-approval` carries no offer identity.** The endpoint takes no body identifying the offer; the server sends whatever the leg's **persisted** shortlist is. The merged dialog re-writes the shortlist immediately before sending, so exposure shrank from unbounded (S5.6) to a few-ms gap **and** now needs a second maker on the same leg concurrently. Assessed **Low**. *Follow-up:* accept `{quoteId, variant}` and verify server-side.
4. **C3 — multi-FF negotiate is not atomic.** N forwarders = N sequential `request-requote` calls, no transaction. A mid-batch failure leaves some asked and some not, and each success has already reissued that FF's portal token and reset their RFQ deadline — not rolled back. The dialog surfaces a per-FF result list rather than hiding it. *Follow-up:* a bulk endpoint taking `{quoteIds[], notes}` in one transaction.

### Outcome
- **`pnpm run ci` GREEN** at `5f5c7bf`: lint + typecheck × 3 workspaces · shared 24 files · web 110 files / 736 tests · api 90 suites / 382 tests · 3 builds.
- **The frontend-only constraint held on EVERY commit** — `git diff --stat 6c5f5d1..5f5c7bf -- apps/api packages/shared prisma` is empty. Verified after each task, not just at the end.
- **Visually verified live** (seeded query `S56VIS-0001`), Executive **and** Manager, **both** grid orientations: route-diagram click opens the leg; FF grouping rules; tinted recommended column/row with the reason on the badge; `Rate (per USD)`; Select→merged dialog with the full four-value summary; leg-level Negotiate listing each forwarder's price + status with ineligible ones genuinely `disabled`; Negotiate blocked at `PENDING_APPROVAL`; Executive sees no checker panel or Generate gate, Manager sees both with Approve/Reject enabled; view-mode preference survived a logout/login as a different role.
- **One defect found by the visual pass alone** and fixed in `5f5c7bf`: `MakerPanel` rendered an **empty bordered card** on every plain `DRAFT` leg (Tasks 4 and 5 removed everything it held, but the container still rendered). No reviewer could catch it — the DOM node was present and nothing asserted on emptiness.

### What the reviews caught (worth knowing — the pattern repeated)
Almost every finding this run was **missing coverage, not broken code**: the implementation verified correct, but nothing would have noticed if it stopped being. Reviewers found them by deleting production code and watching the suite stay green — the rows-view stale badge, the rows-view unpriced guard, `overrideReason` reaching the wire, the send-side error alert, and the negotiate retry-narrowing. Every fix was therefore required to be **mutation-proven**, and the final review found the view-mode desync (item #1) precisely *because* its test had been shaped so it could never fail. Full detail in the SDD ledger.

**Notable:** merging shortlist + send (#4) **structurally eliminated** the Critical S5.6's final review had caught — the drift between the grid's live pick and the persisted shortlist that could award the wrong forwarder silently. Two independent reviewers traced it and confirmed it is now unreachable, not merely guarded; the `unsavedPick` guard was removed and its regression coverage **ported, not deleted**.

**⚠ Observed once, not reproduced:** `award-generate.e2e-spec.ts`'s "combinedUsd is re-rounded" test failed in a single full-suite run, then passed 5/5 on re-runs (isolated and whole-file) and in two subsequent full `ci` runs. Its `rawSum` assertion depends on float summation **order** over `snapshot.legs`. Stage-5 backend, untouched by S5.7 — flagged in case hosted CI ever hits it.

## S5.8 (Client quotation) — ✅ COMPLETE
Pulled forward from Stage 6 at the requester's direction (*"We will finish all the quotations in Stage 5"*). Design: `docs/Stage 5 - Client Quotation - Design.md`. Plan + SDD ledger: `docs/plans/stage-5/Stage 5 - S5.8 …` and `.superpowers/sdd/Stage 5 - S5.8 …/progress.md`. Mockups: `.superpowers/brainstorm/1145-*/content/`.

**What it does.** Once every leg is approved and the award frozen, a Manager opens `/queries/:id/quotation`: a margin % marks up every charge line (markup on cost — `cost × (1 + m/100)`, **not** `cost ÷ (1 − m)`), individual lines can be hand-adjusted and are pinned as overrides, and the quotation is previewed and issued. Issuing freezes a snapshot, renders the client email **server-side**, and moves the query to `AWAITING_CLIENT_DECISION` — wiring up one of the four query statuses that were previously declared but unreachable.

**The client sees the grand total only** — no charge lines, no per-leg totals, no cost, no margin, no forwarder names. Their own enquiry particulars are echoed back instead of our route/scope. That rule is enforced by server-side template rendering, not by the UI.

**Locked decisions:** markup on cost · one margin applied per line with pinned overrides · grand total only · no PDF (removed by that decision) · saved draft, versioned on each issue · USD · Manager+.

### Open items
All of S5.8's open points are folded into the **register at the top of this document** — A1 (quotation validity, the one commercial assumption made on your behalf), C1 (no SMTP delivery), C2 (subject is free text), C3 (Prisma drift), C5 (api e2e flake) and C6 (no visual pass). Nothing S5.8-specific lives only here.

### What the final review caught
Three Criticals invisible inside any single task's diff: **calc charges pricing at $0** (client quoted below our own cost, silently), **no entry point to the builder** (reachable only by typing the URL), and **reopen → re-award → permanent 409**. Plus a UI/server contradiction where a pinned price could be silently unpinned by clicking in and tabbing out — with the test suite encoding that divergence as required. Detail in the ledger.

## S5.9 (Approval Flow & Compare Screen Rework) — ✅ COMPLETE (10 of 10 tasks)
A checker-facing gate on top of S5.4–S5.8's award flow, plus a rework of the compare screen around it. Design: `.superpowers/sdd/Stage 5 - S5.9 - Approval Flow & Compare Screen Rework - Implementation Plan/` (design of record + the 10-task plan). SDD ledger (full per-task detail, every review, every mutation proof): `.superpowers/sdd/Stage 5 - S5.9 …/progress.md` — it is the source of truth behind everything summarised here. Built subagent-driven (TDD, per-task review + fix loops), commits `13625c7..b3834e5` for Tasks 1–9, plus Task 10 (this commit) for the UI polish and this handoff.

**What it delivered:**
- **`PENDING_APPROVAL` leg and quote statuses (Task 1).** Sending an offer for approval now moves *both* the leg and the specific quote to `PENDING_APPROVAL` — previously only the decision record changed, so a shortlisted quote still read `QUOTED` while a checker was reviewing it. One pure rule in `@svyft/shared` (`rollupLegTarget`) derives what the leg's status should be from its quotes' statuses; `LegQuoteProjector` uses it for the normal quote-driven rollup path, and `award.service.ts`'s `legRollupTarget` wraps the same rule (against the *locked* transaction snapshot) for `approve` (as the A8 guard) and `reject` (to restore the rollup) — so the two paths can never disagree (D4).
- **Merged, transactional send-for-approval (Task 3, closes register B2/B3).** `POST send-for-approval` now names the offer directly (`{quoteId, variant}`) instead of trusting whatever the persisted shortlist happens to be, verifies it server-side, and runs inside one transaction that takes a row lock (`SELECT … FOR UPDATE`) on the `Leg` — closing both the missing decision-status guard (B2) and the offer-identity gap (B3). The reviewer reproduced the original double-send race 4/4 before the fix, 0/4 after.
- **Approve/reject drive leg *and* quote status (Task 4, D4).** Approve moves the winning quote to `APPROVED`; reject moves it back to `DRAFT` and — the part worth calling out — restores the leg's rollup to the **recomputed** target (via the same `rollupLegTarget` rule), not merely to whatever it was before the send. That matters because other quotes on the leg can have changed status while the decision was pending; a naive revert would drift from what the leg's other live quotes actually justify.
- **The negotiation guard (Tasks 2 and 5, D5).** Negotiating (asking a forwarder to re-quote) on a leg that's pending approval is now refused end-to-end. **CORRECTED (final whole-branch review, IMPORTANT 2)** — this used to claim it "fell out mostly for free" because a shortlisted quote's own status is now `PENDING_APPROVAL`, so `REQUOTABLE_STATUSES = [QUOTED, APPROVED]` "already excludes it — no new guard needed there". It is closed by a **real, explicit leg-level guard** added in Task 5: `negotiation.service.ts` reads the leg's `LegAwardDecision` and 409s while it is `PENDING_APPROVAL`. The quote-status route alone is insufficient — it gates only the quote named in the call, so a still-`QUOTED` sibling on the same leg would reset a decision under review. See register section D's B1 entry. PO ruling **D5** confirmed this is the intended behaviour (not "accept the reset and drop the UI restriction", the register's other option), and the UI (`NegotiateDialog`'s eligibility list, `CompareLegPanel`'s disabled-reason text) now matches the server instead of contradicting it. Closes register **B1**.
- **The portal token no longer rotates (Task 5, D7/D8).** Each re-fetch of a forwarder's portal used to reissue a fresh `accessToken`; it's now stable, and the raw value is persisted rather than only a hash. Reviewed against an adversarial payload set and found not to widen exposure in isolation — but see register **C8**, a new and unrelated finding this surfaced.
- **The FF-portal re-quote fix (Task 6) — a real, shipped defect, found and fixed.** A forwarder asked to re-quote (a leg whose only live quote is `REQUOTED`) hit the ff-portal frontend and landed in `LegSection`'s "not open for quoting" branch — even though `ff-portal.service.ts`'s submit guard had *already* accepted `REQUOTED` submissions and retained the forwarder's prior draft since S5.5. The whole negotiate feature dead-ended at the forwarder: the backend half was built and e2e-tested, but **there was no ff-portal frontend test for the `REQUOTED` case at all**, which is exactly why nothing caught it before now. Fixed by giving `REQUOTED` the same editable branch as `RFQ_SENT` (commit `a3e0aef`); `LegSection.test.tsx` gained the coverage that should have existed already. The same commit carries **D9**: `PENDING_APPROVAL` and `APPROVED` both render as the neutral "Under review" badge on the forwarder's screen — approval carries no forwarder notification and stays reversible until the client accepts, so the portal must never leak a leading/losing signal either way.
- **The stale-page guard (Task 7, D10).** Portal submit is now guarded by a derived leg version — a hash of the leg's live quote/offer state computed on both sides — so a forwarder tab left open across an intervening change (another forwarder's quote landing, a change-order) can't silently submit against a basis that no longer exists.
- **The compare-screen rework (Tasks 8–9).** Grid column order, a `★` star mark on the recommended cell alongside the existing tint (not a replacement for it), and a forwarder band row for multi-variant offers (Task 8). The maker's flow collapsed to **one** dialog: `SendForApprovalDialog`, opened by a plain button below the whole grid, lists every priced offer and picks + sends in a single call. The old two-step flow — an in-grid `Select` affordance feeding a separate `ShortlistDialog` — is retired entirely (Task 9); its regression coverage (the S5.6-era "picked offer vs. persisted shortlist can drift" Critical) is ported, not deleted, onto the new single dialog.
- **Task 10 (this session, product item 7).** The two locked-state paragraphs `MakerPanel` used to render as standing boxes ("Locked while this leg is pending approval…" / "This leg is approved — its shortlist is final here…") moved onto the leg header's decision chip as a hover tooltip. The rejection alert — the maker's only on-screen cue that rework is needed — deliberately **stayed** a standing, always-visible alert; burying it was the exact defect an earlier review already caught once (S5.6 final review, finding I2). `MakerPanel` now renders only that alert, or `null`.

### Every product-owner ruling made during the build
These are decisions, not implementation details — a fresh session should not need to re-derive or reopen them:
- **D5 — negotiate stays refused while a leg is pending approval.** Not "accept the server-side reset and drop the UI restriction" (the register's other option); the UI and server must agree that pending-approval legs are off-limits to negotiate until a checker rejects.
- **D9 — "Under review" is the forwarder-facing label for both `PENDING_APPROVAL` and `APPROVED`.** The forwarder portal must never distinguish "pending" from "approved" — no leading or losing signal reaches a party that hasn't been notified of anything yet (consistent with the Vocabulary section's "nothing is awarded" rule).
- **D7/D8 — the portal token stops rotating, and the raw token is persisted** rather than only ever hashed. Chosen so a forwarder's bookmarked/emailed portal link keeps working across an unlimited number of re-fetches instead of silently going stale.
- **`version` stays required on the portal submit (Task 7).** The stale-page guard's `version` field is `z.string().min(1)` — genuinely required, not optional-with-a-fallback — even though that means a forwarder tab already open at the moment this ships would get a 400 with no visible error (the pre-deploy bundle only handles 422/409). Ruling: *"current production deployment is not real production so there won't be any FF tab open, currently impact anything"* — accepted, because **there are no live forwarders at the introducing deploy**. The risk is a one-time cost paid once, while nobody is exposed to it; every deploy after this one ships both bundles already sending `version`, so it does not recur.
- **The reason field is always visible; mandatory only off-recommendation, voluntary otherwise (Task 9, M1).** *"field should always visible and make it mandatory if not choosing the recommended option else user can voluntarily fill."* The override-reason textarea in `SendForApprovalDialog` is never unmounted; it's required only when the picked offer differs from the recommendation, and a voluntary reason typed on the recommended path is now persisted rather than silently dropped (the frontend used to spread `overrideReason` into the request body only when required, which would have discarded a voluntary one).

### Final whole-branch review — fix wave (after all 10 tasks)
A whole-branch review over `e2f2898..ef470e4` returned 1 Critical, 4 Important and 10 Minor. Fixed:
- **C1 (Critical, cross-task seam, a regression vs pre-branch).** A leg sent through the **A9 "proceed without waiting"** hatch — genuinely `FULLY_QUOTED`, with one `QUOTED` and one `REQUOTED` quote — could never be approved, and rejecting it made it worse. `approve`/`reject` both asked `rollupLegTarget`, which returns `null` for `[PENDING_APPROVAL, REQUOTED]` because `LEG_ROLLUP_RESOLVED` deliberately excludes `REQUOTED`. So approve 409'd forever (negotiate refused by D5, re-send 409'd by B2), and reject fell through to `RETURN_PARTIAL`, **demoting** the leg to `PARTIALLY_QUOTED` — after which a re-send failed A3. Root cause: `FULLY_QUOTED` here is a **hysteresis** state (the projector never walks an already-`FULLY_QUOTED` leg backwards), so a pure function over current quote statuses can never reproduce it. Fix: one shared `isFullyQuotedForDecision` used by both halves — fully quoted iff the live rollup says so **or** the leg genuinely held `FULLY_QUOTED` when it was sent, read back off the immutable `StatusTransition` row the send's own fire wrote (no migration, no duplicated state). Both terms are individually mutation-proven.
- **I1 (Important).** `SendForApprovalDialog` kept `REQUOTED` offers selectable although the server's in-transaction A1 refresh refuses them unconditionally — a maker could write an override reason, tick proceed-without-waiting, press Send and get "refresh and pick again", advice that cannot help. Stale offers are now listed **disabled with their reason on screen** (`NegotiateDialog`'s ineligible-forwarder pattern); the test that pinned the wrong side of the contract was flipped.
- **I3 (Important).** The forwarder portal's submit handled only 422 and 409 with no `else`, so a 401/403 (staff clicking *Regenerate* while a forwarder has the page open) or any 500 stopped the spinner with nothing on screen — the shape that made Task 7's Critical invisible. Added a revoked-link message and an unconditional catch-all, neither of which relays the server's own string to a forwarder (D9).
- **Documentation and comment corrections.** The B1 closure note recorded reasoning the code explicitly rejects (see section D); `COMPARABLE_STATUSES` was quoted stale in "Approaches that didn't work"; `award.module.ts` still carried a mid-build note claiming `sendForApproval`/`approve` were "broken by this contribution"; `CompareLegPanel.tsx` claimed the server still accepts a re-quote on a `PENDING_APPROVAL` leg, which D5 removed. All corrected in place.

### Accepted risks — carried forward, not closed
- **The B1 wedge (`leg = PENDING_APPROVAL` + `decision = DRAFT`), via a sibling-quote race (Task 5).** A pre-existing race — S5.9 strictly *shrank* its reachable-state set, it didn't introduce it. It is **not terminal** in the state-machine sense: an ordinary change-order legitimately pulls both the leg and the decision back out (the award-change-order listener resets the decision unconditionally, and both `PENDING_APPROVAL --REOPEN--> READY_FOR_RFQ` and `--INVALIDATE--> INVALID` edges exist). But it does **not self-heal** — approve/reject 409 on it, `requestRequote` can't fire the edge needed to escape it, and `sendForApproval` has no edge from `PENDING_APPROVAL` either. A human has to notice and make some qualifying edit (a field change that triggers a change-order) to clear it. Both cheap mitigations were assessed and correctly rejected: locking in-transaction can't roll back a fire that already committed, and a leg-level self-loop can't bypass a quote-level guard.
- **The second post-commit-fire wedge (`decision = PENDING_APPROVAL` + `leg = FULLY_QUOTED` + `quote = QUOTED`), added by the final whole-branch review.** `sendForApproval` commits its transaction and only then fires the quote and leg transitions. If the **quote** fire throws — a change-order invalidating the named quote in the gap between commit and fire — the decision is already `PENDING_APPROVAL` while the quote and leg never moved. From there `approve` 409s on A8 (the quote is not `PENDING_APPROVAL`) and `reject` 409s on its mirrored freshness check, so neither checker action can clear it. Unlike the B1 wedge above, this one **does self-heal**: the very change-order that caused it runs `award-change-order.listener.ts`, which resets this decision unconditionally moments later. Narrow window, no action needed — recorded so it is not re-discovered as new.
- **A latent footgun in the RFQ DTO seam (final whole-branch review).** `DistributeRfqEntry` (`packages/shared/src/rfq.ts`) already carries an **optional `accessToken`**, and since S5.9 T5 `rfq.service.ts` reads full `Rfq` rows — which now include the persisted raw token. Nothing currently spreads one into the other, but a future `{...rfq}` in that file would start returning the raw portal token for every existing RFQ, and **nothing type-checks against it**: the field is optional on the target, so the spread compiles clean. If that seam is touched, map fields explicitly (the convention every other `Rfq` serialization already follows) rather than spreading.
- **The `MessageLog` plaintext finding — new, real, out of scope here.** See register **C8**: `GET /api/queries/:id/emails` returns the rendered forwarder-invitation email (including a working portal link) to any authenticated user, not just Managers/Admins. The final whole-branch review **raised its impact**: that link is an unauthenticated bearer credential for the `@Public` portal, so an Executive can read any query's frozen manifest and submit a quote *as* that forwarder; D7 also removed the only thing that ever aged such links out. **D8 does not widen it** (`rfq.requote_requested` is `entityType: "RFQ"`, this listing filters `QUERY`). Predates this branch entirely; **PO ruling — stays its own open register item (C8), no code change in this branch.**

### Register housekeeping
**B1, B2 and B3 close** (see register section D for exactly how each was closed). **A5 stays open by choice** — S5.9 built and rebuilt the entire approval flow without touching query-level status; a query still reads plain "Quoted" throughout send/approve/reject. **C8 is new** (the `MessageLog` finding above). **C5 gained a third member**: `charge-catalogue.e2e-spec.ts`'s "rejects unauthenticated requests" failed once with a socket-level Parse Error during this session's fix-wave full-gate run — confirmed a flake, not a regression, and not reproduced since.

### Outcome
- **`pnpm run ci` GREEN**: shared 26 files/390 tests · web 113 files/785 tests · api 93 suites/443 tests · lint + typecheck + 3 builds clean. One full-gate run this wave hit the known C5 flake (`charge-catalogue.e2e-spec.ts`, once) — confirmed not a regression: passed 3/3 in isolation and clean in a subsequent independent full `--runInBand` gate.
- Task 10's mutation-proof: restoring `MakerPanel`'s removed paragraph (with its guarding `isLocked` branch reinstated) turned exactly the two new absence tests red (2 failed / 4 passed in that file), confirming both assertions are load-bearing, not vacuous.
- `Badge` (`components/ui/badge.tsx`) became `forwardRef` as part of Task 10 — the decision chip's tooltip trigger uses `asChild` on the badge itself (rather than nesting a second interactive element inside the leg header's own `<button>`), and Radix's `Slot` needs a forwarded ref to anchor the popper to the right DOM node.

### Deferred minors — for the final review and a future session to triage
Full detail and file/line references are in the SDD ledger; listed here so nothing gets lost:
- `award.module.ts`/`award.service.ts` carry a few stale step-numbering and mechanism-naming comments from the send/approve/reject rewrites.
- The A8 approve-guard (an already-decided quote can't be re-approved) has no test that fails without it — it 409s on the rollup guard first. Reachable in production; worth a dedicated test.
- The portal client (both old and new bundles) only handles 422/409 responses — any future contract change that returns something else fails silently on the forwarder's screen with no visible error. This is the same shape that made Task 7's `version`-required Critical invisible; worth fixing before the portal's contract changes again.
- `negotiation.service.ts`'s replacement comment names the wrong mechanism in one spot; `status.ts`'s event doc still describes `QUOTE/APPROVED -> REQUOTED` though a `PENDING_APPROVAL` edge has existed since Task 2.
- Band-row tests (Task 8) only exercise the first forwarder in a multi-variant leg; the recommendation star nests inside the clickable header button, so its `aria-label` concatenates into the button's accessible name (accessibility follow-up).
- The USD total in `SendForApprovalDialog` sits outside the radio's `<Label>`, so it isn't part of the accessible name — relevant because disambiguating similarly-named forwarders by total is the whole reason the totals are shown.

## S5.9.1 (Compare Screen Review Fixes) — ✅ COMPLETE (5 of 5 tasks)
Nine review points the product owner raised against the delivered S5.9 screen, plus a tenth ask ("on rejection… it should go directly to executive queue", R7) and one hardening task found live mid-build (Task 5). Design/plan: `docs/plans/stage-5/Stage 5 - S5.9.1 - Compare Screen Review Fixes - Implementation Plan.md`. SDD ledger + task briefs/reports: `.superpowers/sdd/Stage 5 - S5.9.1 - Compare Screen Review Fixes - Implementation Plan/`. Built subagent-driven (TDD, per-task review), commits `640ab33..de22dba` for Tasks 1–3 and 5, plus Task 4 (this session's notification) and this handoff.

**Coverage of the nine points** (design doc's own mapping, `Self-review notes`): #1/#2 → Task 1 (R1 snapshot-based `★`, R2 recommendation vs. pending-approval as independent signals); #3 → Task 2 (R3, Negotiate hidden for Manager/Admin); #4/#5/#6 → Task 2 (R5, Approve/Reject move into the action bar behind confirmation dialogs); #7 → **already correct in S5.9** (reject restores the status the leg actually left, via `legStatusWhenSentForApproval`) — no task needed; #8 → Task 3 (R6, columnar alignment + density); #9 → Task 1 (R4, decision chip removed). R7 (the notification) is additional to the nine — a separate product-owner ask bundled into the same follow-up.

- **Task 1** (`99cff28`) — the `★` now reads the decision's **snapshotted** recommendation (`decision.recommendedQuoteId`/`recommendedVariant`) once a decision exists, falling back to the live `leg.recommendation` only when one doesn't. **R1's correction, worth stating precisely:** S5.9 didn't have a wrong root cause — the server's live ranking genuinely only ranks `QUOTED` quotes, so it can drift to a different forwarder after a send, and *something* had to stop the marker from following that drift. What S5.9 got wrong was the *fix*: it set `recKey = null` outright whenever `decision.status !== "DRAFT"`, so a checker reviewing a sent leg saw **no recommendation at all** rather than the one the maker actually acted on. The decision already snapshots `recommendedQuoteId`/`recommendedVariant` at send time — reading that instead of suppressing was always available. **Verified live** on `S56VIS-0001`'s leg L1: `decision.status = PENDING_APPROVAL` with the *live* recommendation `null` (nothing currently ranks), and the `★` still renders off the snapshot — pre-Task-1 this exact state showed no recommendation at all, which is the product owner's complaint reproduced and fixed on real data. Task 1 also removed the decision chip (R4, #9): `LegStatusBadge` is now the single status shown in the leg header. **R4's accepted consequence:** a rejected leg's header row is now visually indistinguishable from one that was never sent — the two-badge duplication the product owner objected to is gone, but so is the one glance-level cue that a leg was rejected. The reason lives inside the leg body (`MakerPanel`'s standing alert, unchanged from S5.9), and **R7's new notification (Task 4) is now the surfacing mechanism** for the fact itself, not just the reason.
- **Task 2** (`5b7f547`, `fe8939c`, `258b92e`) — checker actions (Approve/Reject) moved out of `CheckerPanel` and into the leg's action bar, each behind a confirmation dialog (`ApproveDialog`/`RejectDialog`) that names the shortlisted offer, not the recommended one — a reviewer-mutated test (pointing the dialog at `recommendedQuoteId` instead) keeps all 161 compare tests green unless a fixture is added where the two diverge, which the fix round closed. **Negotiate is hidden for Manager/Admin** (R3): their only path to change a leg's outcome is Reject-with-reason. **Manager/Admin keep "Send for approval"** — so a Manager can still act as maker on one leg and, on a *different* leg (four-eyes: `sentByUserId !== user.id`), act as checker; the maker/checker split survives, it's just that a Manager never negotiates. `CheckerPanel.test.tsx` was renamed to `GenerateGate.test.tsx` (it now holds only the Generate-gate tests, everything else moved).
- **Task 3** (`da0574d`) — the columnar grid's cell alignment now matches its header alignment (previously mismatched — the "data not under the header" complaint), padding and row height reduced so the table reads denser, and the table sizes to its content instead of stretching to fill the wrapper. Rows view is unaffected outside its own already-correct right-alignment. Visually verified live on `S56VIS-0001`.
- **Task 5** (`de22dba`, added mid-build, not one of the nine) — a `⚑` marker beside the `★`, deriving "sent for approval" from `decision.shortlistedQuoteId` rather than the offer's own `quoteStatus`. **Why this was needed, and why it is not a bug report against S5.9's code:** the controller found, live on `S56VIS-0001` L1, a row where `decision.status = PENDING_APPROVAL` and `shortlistedQuoteId` was set, but that quote's own `quoteStatus` still read `QUOTED` — so nothing on screen identified the offer under review. **That specific row is stale pre-S5.9 seed data, not a code defect**: the real `send-for-approval` endpoint does fire the quote's status transition, and that is e2e-proven (`award-workflow-maker.e2e-spec.ts`). The finding is real regardless — a second-hand signal (`quoteStatus`) is the wrong source for a decision-critical marker precisely because it *can* drift from the decision that is supposed to be authoritative, seed artifact or not — so the marker now reads the same field (`decision.shortlistedQuoteId`) Task 1 already uses for the recommendation, for symmetry and drift-immunity. Verified live: the drift-case row now renders "Dedicated★⚑" with both accessible names, the other five rows carry no marks, and the shared footnote explains both glyphs on one line.
- **Task 4** (this session) — a rejected leg now notifies Executives. **The routing problem the product owner named ("I want it should go directly to executive queue") was already solved**: there is no assignment concept anywhere in the schema, and `reject()` already cleared `sentByUserId` and wrote the decision back to `DRAFT` before this task touched anything — a rejected leg genuinely returns to the general maker pool, not to whoever sent it. What was missing was that **nothing announced it**: every other significant event in the system (`quote.received.inapp`, `rfq.expiry.inapp`, `rfq.requote_requested.inapp`) had a notification and a rejection did not. Fixed by adding a comms block to the **end** of `award.service.ts`'s `reject()` — after the decision write and both post-commit status fires — mirroring `ff-portal.service.ts`'s post-submit comms block exactly: resolve `user.findMany({role: EXECUTIVE, isActive: true})`, dispatch a new `award.rejected` event `IN_APP` only (no forwarder-facing side to a reject), and wrap the whole block in `try/catch` + `logger.error` so a comms failure can never fail an already-committed, already-fired reject. New template `award.rejected.inapp` added to `message-templates.seed.ts`. **No migration was needed**, and this was verified rather than assumed: `seedMessageTemplates`'s upsert is create-only (`update: {}`), it runs on every deploy via `reference-seed.ts` → `deploy.yml`'s post-`migrate-deploy` seed step, and a brand-new template *key* is created fine by a create-only upsert on any database, deployed or fresh-CI, that runs the seed — so the new row reaches every environment on its next ordinary deploy with no extra step. Tests (`award-workflow-checker.e2e-spec.ts`, 2 new): one proves an Executive the test itself seeds receives a `Notification` row typed `award.rejected` scoped to the query; the other deletes the template row first (forcing `NotificationDispatcher.dispatch`'s `IN_APP` lookup to return `null`, i.e. nothing to send) and asserts the reject still lands the decision at `DRAFT` — proving a comms-side no-op/failure can never surface as a failed reject. The reject-endpoint status code in the plan's illustrative test (`.expect(201)`) doesn't match the controller's actual `@HttpCode(200)`; the shipped test uses the real `200`.

### Outcome
- **`pnpm run ci` GREEN**: shared 26 files/390 tests · web 115 files/832 tests · api 93 suites/445 tests (+2 over S5.9's 443, both new) · lint + typecheck + 3 builds clean. No C5 flakes hit in this run.
- Task 1's mutation proof: the three-branch recommendation resolution (decision absent → live; decision present with a snapshot → snapshot; decision present with a null snapshot → nothing, deliberately not falling back to live) was verified branch-by-branch against the code, including the null-snapshot branch that must **not** fall back.
- Task 5's mutation proof: deriving the `⚑` from `offer.quoteStatus` instead of `decision.shortlistedQuoteId` reddened 11 tests, including the dedicated drift-case test built specifically to catch this.
- Task 4's evidence: the "notifies executives" test was run red-then-green (failed with 0 notifications before the comms block existed, passed after); the "comms failure never fails reject" test passed unmodified either side, since `NotificationDispatcher.dispatch` treats a missing template as nothing-to-send rather than a throw — the try/catch is defensive-in-depth matching `ff-portal.service.ts`'s own precedent, not something this particular test forces to fire.

## Reusable facts (learned across S5.4/S5.5 SDD)
- Caller id = `user.userId` (`RequestUser`); e2e actor `@db.Uuid` cols need cookie `sub: randomUUID()`.
- Every owned status change via `StatusService.fire` (awaits `emitAsync` → projectors run in-fire → **every new `@OnEvent` listener MUST try/catch+log** or it fails the caller). Query status is a projection (never `fire`d).
- Clear a `Json?` column with **`Prisma.DbNull`** (plain `null` sets JSON-null, still truthy).
- Local Postgres :5433 (`svyft-postgres-task4`); `set -a; . apps/api/.env; set +a` before prisma/jest.
- Rebuild `@svyft/shared` after editing it; run `pnpm run typecheck` per task (vitest/jest don't type-check).
- **(S5.9)** A `StatusService.fire` call MUST happen **after** an outer transaction commits, never inside one that holds a row lock on the same row `fire` needs — `fire` opens its own transaction on a different pool connection, so firing from inside a lock-holding transaction self-deadlocks until Prisma's 5s interactive-transaction timeout (P2028).
- **(S5.9)** To serialize two HTTP-triggered mutations that must not race on the same row, take the **same** `SELECT … FOR UPDATE` lock, in the same order, in both call paths — mutual exclusion is a property of the shared lock helper (`lockLeg`), not of either caller individually, and needs no dedicated concurrency test once that's true.
- **(S5.9)** A plain function component (no `forwardRef`) silently breaks under Radix's `asChild`/`Slot` pattern — React drops the ref with only a dev-console warning, not a crash, so it can ship unnoticed. Any `components/ui/*` primitive that might become a `TooltipTrigger`/`PopoverTrigger`/etc. child should be `forwardRef` from the start (`Button` already was; `Badge` wasn't, until S5.9 T10).
