# Stage 3 — Session Handoff & Progress

> Resume point for a fresh session. Points to the deliverables and records what's done + what's next so nothing is re-litigated.

## Current status (2026-07-20)
- ✅ **Functional spec** — `Stage 3 - Create Query - Functional Spec.md` (behaviour; decisions D1–D12). *The what.*
- ✅ **Technical design** — `Stage 3 - Technical Design.md` (architecture, data model, APIs, extensibility core, deployment). *The how.*
- ✅ **Plan 0 (Foundation)** — merged + **deployed LIVE**. `docs/plans/stage-3/2026-07-16-plan-0-foundation.md`.
- ✅ **Plan 1 (Auth & RBAC)** — merged (PR #2) + deployed. JWT access token in an httpOnly cookie + rotating refresh (SHA-256-hashed in `RefreshToken`); global `JwtAuthGuard`+`RolesGuard` (Executive/Manager/Administrator); seeded users; login UI. `docs/plans/stage-3/2026-07-17-plan-1-auth.md`.
- ✅ **CD fix** — merged (PR #3). Repo `docker-compose.prod.yml` matches the live `:4096` HTTP stack; deploy uses `up -d --force-recreate`.
- ✅ **Plan 2 (Masters & Reference Data)** — merged (PR #4/#6) + cleanup (PR #5) + deployed. Clients (+contacts API), Vessels, Config (density factors + checklist); masters/admin UI; idempotent reference-data seed; global `PrismaExceptionFilter`. `docs/plans/stage-3/2026-07-17-plan-2-masters.md`.
- ✅ **Plan 3 (Extensibility Core)** — merged (PR #8) + deployed. The reusable framework (Technical Design §7): the **Status Machine** (`status` module) + the **Change-Impact mediator** (`changes` module). `StatusTransition` log + migration #3. `docs/plans/stage-3/2026-07-18-plan-3-extensibility.md`.
- ✅ **Plan 4 (Query aggregate + Cargo)** — merged (**PR #9**) + deployed. The **Query root** (`queryCode` `YALYY-NNNN` from a row-locked `QuerySequence`; contact/vessel snapshots; `dgIndicator` synced; system-only `status`), **Cargo** (atomic rows; DG + MSDS PDF; `volumeCbm` **generated column**; density/chargeable weight null), the **completeness checklist**, and **`FileAsset`** (storage-backed). Wires the Extensibility Core: `Query.status` persisted via `QueryStatusProjector`, every Query/Cargo edit through `ChangeMediator` (Free path), Query/Cargo impact classes declared. Migration #4 (`query_cargo`). `docs/plans/stage-3/2026-07-18-plan-4-query-cargo.md`.
- ✅ **CD fix #2** — merged (**PR #10**). Deploy now **auto-seeds reference data** (`reference-seed.cli.js` after `migrate deploy`, idempotent) and `docker tag`s the deployed image as local `:latest` — closing the "empty ChecklistDefinition/FreightDensityFactor in prod" gap and the stale-`:latest` footgun (below).
- ✅ **Plan 5 (Points/Legs/Route Engine)** — merged (**PR #12**) + deployed + **prod-verified end-to-end** (2026-07-19). The reusable **`Point`** graph (single-table, 5 types; per-type required fields in Zod), **`Leg`** (`legCode` `L1/L2…`, never reused; per-leg `mode` reusing `FreightMode`; owned `status`; `executionStatus`; `totalChargeableWeight` null) / **`LegCargo`** (D7 join; `manifestSnapshot` reserved), the pure isomorphic **`validateRoute(graph, phase)`** engine (full §10 catalogue), the **`points`/`legs`/`routing`** modules, and all six reserved Plan-3/4 seams wired. Migration #5 (`points_legs_route`). `docs/plans/stage-3/2026-07-19-plan-5-points-legs-route.md`.
- ✅ **Plan 6 (Wizard & Query List UI)** — merged (**PR #13**) + **deployed** (CD Deploy success) + **live** on `:4096`. Stage-3 frontend: `GET /api/queries` list endpoint + shared `QueryListRow`/`QueryDetail`; app-wide shadcn **design system** (`frontend-design`) + Stepper/DataTable/`ApiError`/`FindingsPanel`; the **Query List** (`/queries`); the stepped **Create-Query wizard** (WizardShell + save/mint model — first Save OR first leg-add mints the ID; per-step PATCH); Steps 1–5; the **Step-4 leg/route builder** (5-type PointEditor, D7 CargoAssignment, LegEditor + V-M1 client-precheck & server-422) + the signature **RouteDiagram** (bespoke SVG running the isomorphic `validateRoute`); the Create Query flow (client preview → optional-gaps prompt → server-422 → `RFQ_READY`). Subagent-driven; the opus whole-branch review caught+fixed a **Critical** (non-admin Step-1 403 — `queryDate` leaked into PATCH) + **Important** (Step-2 double-PATCH). `pnpm run ci` green (**373 tests**: shared 111 · web 157 · api 105). `docs/plans/stage-3/2026-07-19-plan-6-wizard-ui.md`. **⚠ NOT yet full-stack-verified — the build session had no DB. Manual/prod testing IS THE NEXT STEP → see "Plan 6 — Testing & Fix" below.**
- 🔜 **Next: finish Plan 6 testing + fix issues, THEN Plan 7 — Notifications / Escalations / Emails** (resume prompt at the bottom). **⚠ GATE:** Plan 7 *implementation* starts only after Plan 6 testing is green (blocking bugs fixed + a clean prod smoke-test). Plan 7 *brainstorming/planning* may run in parallel — it touches no Plan-6 code.
- Live: `http://142.93.220.226:4096` — HTTP only, no domain/TLS. **Plan 6 now live** (Deploy #13; `GET /api/queries` → 401 = endpoint present; SPA serves the wizard). **No new migration in Plan 6** (frontend + a read-only endpoint). Plan 5's happy-path was previously smoke-verified end-to-end.

## Read order for a fresh session
1. **This file** — current state, conventions, and (for the immediate task) the **"Plan 6 — Testing & Fix"** section + the **Known Issues Register**. Start there.
2. `Stage 3 - Create Query - Functional Spec.md` — the *what*. **Plan 6 testing:** §5 (wizard flow + Save model), §6 (Query List), §7 (wizard sections incl. §7.4 the leg/route builder), §10 (validation catalogue), §13 (form actions). **Plan 7:** §13 (Send Follow-up / Send Acknowledgement), §14 (escalation & notifications), §15 (email templates).
3. `Stage 3 - Technical Design.md` — the *how*. **Plan 6:** §9 (frontend). **Plan 7:** §8.2 (escalation scheduler), §8.3 (notifications polling), §8.7 (email compose-&-log), §2.2 (`notifications`/`escalations`/`emails` modules).

## Roadmap (each plan = its own branch → PR)
Plan 0 Foundation ✅ · Plan 1 Auth & RBAC ✅ · Plan 2 Masters & Reference Data ✅ · Plan 3 Extensibility Core ✅ · Plan 4 Query + Cargo ✅ · Plan 5 Points/Legs/Route Engine ✅ · **Plan 6 Wizard & Query List UI ✅ (deployed; testing in progress)** · **Plan 7 Notifications/Escalations/Emails (next — after Plan 6 testing is green)** · Plan 8 Completion & hardening.

## Plan 6 — Testing & Fix (⭐ NEXT SESSION STARTS HERE)

Plan 6 is deployed to `:4096` but was **never runtime-verified on the full stack** (the build session had no DB). This manual testing IS Plan 6's verification. **Gate for Plan 7:** no known blocking / broken-flow bugs + one clean prod smoke-test.

**Prod access for testers.** The app is auth-gated → seed/share a login (`SEED_*` in the droplet `/opt/svyft-logistics/.env`; seed with `docker compose -f docker-compose.prod.yml exec api node /repo/apps/api/dist/seed/seed.js`, use `exec` not `run`). To build a query you need ≥1 **ACTIVE Client** + contact (create via Masters, or reuse the Plan-5 residue `CL-0002`). Reference data (density/checklist) auto-seeds. It's plain HTTP on the non-standard port `:4096` (some corporate networks block odd ports → try a phone hotspot). `:4096` is publicly reachable (verified from an external host).

**Test hardest (the untested-in-build hot paths):**
- Full **create → RFQ_READY**: Step 1 (client/contact/vessel pickers, mandatory fields, E.164 phone) → **Save** (mints `YAL…`) → Step 2 Incoterms + DG → Step 3 cargo rows (CBM auto-calc, reference tags) → Step 4 points + a multi-leg route + cargo assignment → **Validate route** → **Create Query**.
- **MSDS PDF upload** (multipart, real `uploads` volume) + the DG→MSDS reveal; **Export to Excel** (POST → `.xlsx` blob download).
- The **RouteDiagram** on a real multi-leg route (layout, mode colours, broken-chain / orphan highlight) — only ever fixture-screenshotted.
- **V-M1**: a Sea leg between two street-address (pickup/delivery) points must block; Road clears. Draft warnings vs Create-Query blocking; the optional-gaps prompt (Cancel / Save Draft / Send Anyway).
- **Query List**: search / filters (status, priority, freight mode, country, date range) / sort / pagination; the derived FreightMode/Origin/Destination columns.
- **Roles**: non-admin (Executive/Manager) can save **every** step incl. Step 1 (the fixed 403); Query Date is read-only unless Administrator.
- **Mobile / responsive + keyboard focus** (the a11y pass was code-only — eyeball on a real device).

**Fix workflow.** Blocking / broken-flow bugs → **one small PR each** (`fix(...)`) → CI → merge → CD auto-redeploys → re-test. Batch cosmetic/minor items into Plan 8 (hardening) or a small `chore(web)` polish PR. Keep the loop tight; don't pile bugs into one PR. Use the subagent-driven flow for anything non-trivial, or fix directly for one-liners. Reminder: `.superpowers/sdd/` is **gitignored** scratch that starts fresh each plan — **this handoff + the plan docs are the durable record.**

**Testing round 1 — fixes landed on `feat/plan-6-wizard-ui-bugs`** (2026-07-20; static per-screen validation audit + user's manual hot-path findings). One branch, TDD, `pnpm run ci` green (**398 tests**: shared 125 · web 168 · api 105):
- **Schema guards (`@svyft/shared`):** readyDate ≤ targetDelivery (query + leg), leg origin ≠ destination (no self-loop), list dateFrom ≤ dateTo, trim + reject whitespace-only required text (query contact, cargo, point fields; `collectCreateFindings` treats whitespace as missing).
- **Pickers / codes (web):** vessel search restricted to `status=ACTIVE`; IATA/ICAO/UN-LOCODE auto-uppercase on input (was CSS-only → lowercase failed the regex).
- **Session timeout (web):** a 401 from a protected request now logs out + redirects to `/login` — global handler in `raise()` (`lib/api.ts`) → `AuthProvider` clears user + query cache → `ProtectedRoute` redirects. Auth endpoints excluded.
- **Step-4 points (web):** a **Points list** (type + name + address) with per-point **Edit/Remove** + "Add point" (PointEditor edit mode + `usePoints.update` were built but UI-unreachable; leg rows only ever showed the point name, never the address).
- **Wizard UX (web):** Cancel → Queries list (all steps); Save shows a success notice; all errors/findings surfaced at the **top of the page**; **Next blocks on incomplete mandatory fields** (Steps 1 & 2 — client/contact/dates, Incoterms; stepper stays free, Create Query remains the hard F1 gate); Step 2 gained client-side validation; "Save Draft" now persists; Create/priority failures surface instead of being swallowed.
- **Deferred by decision (Plan 8 / follow-up):** tighten phone to require a leading `+` (D1); make Net ≤ Gross an inline **warning** per §10 F5 instead of a hard block (D2); restrict ROAD leg endpoints to pickup/delivery/warehouse/drayage per §10.4 (D4); field-map server-400 Zod `issues` to individual inputs (G6). Bigger backlog: country dropdown (`PointEditor` TODO), `packageType` enum, HS-code format, IMO check-digit.
- **⚠ Not yet browser-verified end-to-end** — fixes are unit/integration-tested + CI-green; a prod smoke-test of the hot paths is still the closing step before Plan 7.

**Testing round 2 — Legs/Route rework** (same branch, 2026-07-21; testing-team findings; recorded in `docs/Stage 3 - Req & Issues - Round 1 - Design.md`). Step 4 shifts from draft-lenient to **complete-as-you-go**:
- **Add/Edit Point & Leg hard-block incomplete saves** — per-type point fields (`POINT_REQUIRED_FIELDS`); leg origin/destination/mode/≥1 cargo/ready/target — overriding the D8 partial-draft allowance for points/legs.
- **Findings at create-phase** (every route issue is blocking), live client-side + authoritative server validate on Save/Next. New `groupFindingsByScope` buckets findings by box (cargo findings fan onto carrying legs).
- **Screen reordered** Notices → Points → Legs → Route; the Blocking/Advisory `FindingsPanel` list is replaced by a **top summary strip** (query-scoped findings + count) + **per-box ⚠ hover tooltips** on the Point/Leg cards *and* the diagram nodes/edges. The "Validate route" button is removed (validation runs on Save/Next).
- **Save/Next gate**: Save persists + surfaces findings; **Next blocks advancing on any create-phase route error** ("Resolve the route issues…"). `pnpm run ci` green (web 177 · shared 125 · api 105).

## What's built so far (the foundation Plan 7 extends)
- **Monorepo** (Plan 0): `apps/api` (NestJS + Prisma), `apps/web` (Vite/React/Tailwind/shadcn/TanStack Query), `packages/shared` (isomorphic zod schemas + types + enums + the `validateRoute` engine). Docker + GitHub Actions CI/CD.
- **Auth/RBAC** (Plan 1): global secure-by-default guards + decorators in `apps/api/src/modules/auth/` — `@Roles(...Role[])`, `@Public()`, `@CurrentUser()` (→ `RequestUser {userId, role, tenantId}`); `JwtStrategy` (cookie); `AuthService`. `Role` enum + auth contracts in `@svyft/shared`.
- **Masters** (Plan 2): `clients`/`vessels`/`config` modules; reusable `ZodValidationPipe`; **global `PrismaExceptionFilter`** (maps P2023→400, P2025→404, P2002→409; **P2003 NOT mapped** — guard FKs in services); `CodeSequence`-based `CL-`/`VS-` minting; masters/admin web UI.
- **Extensibility Core** (Plan 3, fully WIRED by Plans 4–5): the framework domain modules plug into (Technical Design §7). `status` (`StatusService.fire` — the one guarded door; key-aware `DispatchingStateStore`; leg machine `DRAFT ↔ READY_FOR_RFQ`; `QueryStatusProjector` rollup). `changes` (`ChangeMediator.apply`; per-entity `ImpactRegistry`; async `ImpactClassifier` w/ cargo→leg fan-out; `FreePathStrategy` built; `ChangeOrderStrategy` stub-unreachable = Stage 4). `@svyft/shared` state-machine primitives + `deriveQueryStatus` + `ImpactClass`/`decidePath`.
- **Query aggregate + Cargo** (Plan 4): `queries`/`cargo`/`files` modules — mint `queryCode`, seed 9 checklist items, contact/vessel snapshots, `dgIndicator` sync, derived-on-read `freightMode`/origin/destination + leg roll-ups (`shapeQuery`), PATCH via `ChangeMediator`, Create Query → §10 catalogue → fire DRAFT legs → `RFQ_READY` (idempotent); cargo mediated CRUD + MSDS multipart PDF + `exceljs` export; `FilesService.storeMsds` behind `StorageService`.
- **Points / Legs / Route Engine** (Plan 5): `points`/`legs`/`routing` modules + the shared engine. `PointsService` (5-type single-table CRUD via mediator), `LegsService` (`legCode` `L1/L2…` never reused; `assignedCargoIds`→`LegCargo`; V-M1 pre-write 422; owns leg machine + impact map), `RoutingService` (reads Prisma directly to break a cycle; runs `validateRoute`; `POST /queries/:id/validate?phase=draft|create`). `@svyft/shared` `points.ts`/`legs.ts`/`route.ts` — the pure isomorphic `validateRoute(graph, phase) → Finding[]` (full §10 catalogue; reusable in the browser).
- **Frontend — Wizard & Query List UI** (Plan 6): `apps/web` — app-wide shadcn **design system** (`frontend-design`: Space Grotesk / Inter / IBM Plex Mono self-hosted, cobalt primary + reserved marigold accent, light+dark tokens) + `Stepper`/`DataTable`/`ApiError`/`dedupeFindings`/`FindingsPanel`/`renderWithProviders`. The **Query List** (`/queries` — TanStack Table, server-driven search/filter/sort/paginate). The **Create-Query wizard** (`WizardShell` header+stepper+action-bar+findings; save/mint model — first Save OR first leg-add mints the ID; per-step PATCH; Steps 1–5). The **Step-4 leg/route builder** (`PointEditor` 5 types, `CargoAssignmentControl` D7, `LegEditor` V-M1 dual-path, the signature **`RouteDiagram`** SVG running the isomorphic `validateRoute` for live draft findings + broken-chain/orphan highlights). The **Create Query** flow (client preview via `collectCreateFindings`+`validateRoute("create")` → optional-gaps prompt → server-422 → `RFQ_READY`). **Backend delta:** `GET /api/queries` (search/filter/sort/paginate; derived-field filters pushed to SQL; batched `assignedUserName`) + shared `QueryListRow`/`QueryDetail`. **No new migration.**
- **Prisma models:** `User`, `RefreshToken`, `Client`, `ClientContact`, `Vessel`, `FreightDensityFactor`, `ChecklistDefinition`, `CodeSequence`, `StatusTransition`, `Query`, `CargoItem` (`volumeCbm` generated), `QueryChecklistItem`, `FileAsset`, `QuerySequence`, `Point`, `Leg` (owned `status`), `LegCargo`. Every table has a nullable `tenantId`. **Migrations:** `init_auth`, `masters_config`, `status_transition`, `query_cargo`, `points_legs_route` (#5). The **whole Stage-3 API + the Create-Query UI is built** — **Plan 7 adds notifications/escalations/emails** (the `Notification`/`Escalation`/`EmailLog` models + a migration are still TODO).

## Stack
pnpm monorepo · NestJS 10 (+ `@nestjs/event-emitter`) + Prisma 5 (Neon Postgres) · `exceljs` (export) + `@types/multer` (uploads) · Vite + React 18 + Tailwind + shadcn/ui + TanStack Query **+ TanStack Table** + React Hook Form + Zod · `@radix-ui/*` + `cmdk` + `react-day-picker`/`date-fns` + self-hosted `@fontsource` (Plan 6) · Docker + GitHub Actions CI/CD. Repo: `github.com/sj132q/svyft-logistics`. Local dev: `README.md` (Docker Postgres on **`:5433`** here; `apps/api/.env`; after install `prisma migrate deploy` + `prisma db seed`). **Plan 7 will add** `@nestjs/schedule` (escalation cron).

## Deployment facts
- Droplet `142.93.220.226` (Bangalore), Docker, **shared** with another app (on `:5173`). Our app: HTTP on **`:4096`** (no domain/TLS yet). `:4096` is publicly reachable.
- Neon Postgres (Singapore `ap-southeast-1`); pooled+direct URLs + `JWT_ACCESS_SECRET` + `SEED_*` (**prod overrides the dev defaults**) + `COOKIE_SECURE=false` + `UPLOADS_DIR` live in droplet `/opt/svyft-logistics/.env` (NOT in the repo).
- **Prod compose = `docker-compose.prod.yml`** (HTTP `:4096`; api image `${IMAGE:-ghcr.io/…:latest}`; MSDS `uploads` named volume → `/uploads`). Caddy/HTTPS variant is `docker-compose.caddy.yml` (for when a domain lands).
- **CD** (`deploy.yml`): pushing runtime code (`apps/**`, `packages/**`, `prisma/**`, `pnpm-lock.yaml`) to `main` → build image (tag = git SHA **and** `:latest`) → GHCR → SSH deploy: `pull` → `docker tag $IMAGE …:latest` → `migrate deploy` → `reference-seed.cli.js` (idempotent) → `up -d --force-recreate`. Docs/config pushes do **not** deploy (path-filtered).
- **⚠️ Deploy SSH step is flaky** (Plan-5 deploy): `appleboy/ssh-action` failed once on `dial tcp :22: i/o timeout`; a `--failed` re-run succeeded. To recover: confirm `:22`, then `gh run rerun <deploy-run-id> --failed`. (Add a retry/timeout — see Known Issues.)
- **Reference data auto-seeds on every deploy** (PR #10). **User seed is manual** (needs `SEED_*`): `docker compose -f docker-compose.prod.yml exec api node /repo/apps/api/dist/seed/seed.js` (use `exec`, not `run`).
- Secrets set: `DROPLET_HOST`, `DROPLET_USER`, `DROPLET_SSH_KEY`, `GHCR_TOKEN`.

## Conventions & key learnings (reuse in Plan 7+)
- **Flow:** brainstorm → write plan (`superpowers:writing-plans`) → subagent-driven execution (`superpowers:subagent-driven-development` — fresh implementer per task, TDD, per-task spec+quality review, **opus** whole-branch review) → finish via PR → **verify in prod**. New branch per plan (`feat/plan-N-<name>` off `main`); plan doc + implementation ship as one PR. Ledger `.superpowers/sdd/progress.md` (gitignored — fresh each plan). Models: **sonnet** implementers + task reviewers; **opus** final review + design-heavy tasks. **Proof it works:** the Plan-6 opus whole-branch review caught a Critical (non-admin Step-1 403) + Important (Step-2 double-PATCH) that per-task reviews + green tests missed.
- **Extensibility Core golden rules (enforced by real code):** never hand-write a status — go through `StatusService.fire` or `QueryStatusProjector.recompute`; never raw-write a user-editable field — route through `ChangeMediator.apply(req, uow)` (declare its impact class). Type each impact map `Record<keyof …Input | "@create" | "@delete", ImpactClass>` for compile-time completeness.
- **Owned status columns** (`Leg.status`): authoritative column written in the same tx as the `StatusTransition` log via `DispatchingStateStore`. Tests create real `Query`+`Leg` fixtures (a synthetic id → P2023/P2025).
- **Derived-on-read:** `Query.freightMode`/origin/destination + per-leg roll-ups computed in `shapeQuery` (never stored). The isomorphic `validateRoute` is the single §10 source — the browser runs it too.
- **Soft user refs:** `assignedUserId`/`uploadedById`/`actorId` are plain nullable `@db.Uuid`, no relation. e2e tests use a **UUID jwt `sub`**.
- **Validation:** bind Zod at param level. `PrismaExceptionFilter`; **P2003 (FK) NOT mapped** → verify referenced ids in the service.
- **Shared enums:** `const` object + union + `Object.values(...) as [X,...X[]]` array pinned by a `toEqual` test. Never a TS `enum`.
- **`cookie-parser` import:** e2e specs use the **default import** (namespace import throws under `esModuleInterop`).
- **API CI gotcha:** CI runs a **fresh migrated-but-UNSEEDED** Postgres → every test **seed-independent + self-cleaning** (own reference data + own rows by a unique prefix; a query delete cascades). Verify vs `prisma migrate reset --force --skip-seed` + `pnpm run ci` before pushing.
- **⚠ Frontend conventions (Plan 6 — reuse for any Plan-7 UI):**
  - **HTTP:** `fetchJson`/`postJson`/`patchJson`/`del` from `@/lib/api` (cookie auth; `ApiError` parses 422 `{findings}` + 400 `{issues}`; guards 204/empty).
  - **Server cache:** TanStack Query keyed `["<feature>", params]` / `["<feature>", id]`; mutations invalidate `["query", id]` and rely on the **re-GET** (`shapeQuery` graph) for derived fields/rollups/`assignedCargoIds` — never trust write responses for those.
  - **Forms:** RHF + `zodResolver(@svyft/shared schema)` + shadcn `Form*` (`FormItem`/`FormLabel` **throw** outside `<FormField>` — use plain `div/label` for non-fields). Datetime fields use the floating-wall-clock `toIsoOffset`/`isoToLocalInput` pair (TZ-independent, digit-stable).
  - **Findings** render through ONE `<FindingsPanel>` (deduped via `dedupeFindings`); draft warns, create blocks.
  - **⚠ Web CI:** run `pnpm --filter @svyft/web typecheck` (or `pnpm run ci`) — **`vite build`/esbuild SKIPS type errors** (this hid a real typecheck failure in Plan 6).
  - **Web tests:** Vitest + `renderWithProviders` (`@/test/renderWithProviders`) + `mockFetch` (`@/test/mock-fetch`); stub `/api/auth/me` + endpoints; `afterEach(vi.unstubAllGlobals())`. Radix Select is **jsdom-flaky** → drive the hidden native `<select>` / assert values another way; `test/setup.ts` has ResizeObserver + pointer-capture + scrollIntoView no-op polyfills. Use valid UUIDs for `.uuid()` fields.
  - **Dev server:** `vite.config` aliases `@svyft/shared` → its TS source (else the CJS build breaks the native-ESM dev server). `.claude/launch.json` (gitignored) has a `web` preview config for the Claude_Preview tools.
- **Ops lesson (PR #10):** use `docker compose exec api …` for one-off prod tasks (not `run`). Probe live routes by their real path — 401 = route exists + guarded; 404 = route absent (e.g. `/api/queries` was 404 pre-Plan-6, now 401).

## Known Issues Register (Plans 0–6)

Nothing **open** is a live "broken-in-prod" blocker — the two real Plan-6 bugs (Critical non-admin 403, Important double-PATCH) were caught by review and fixed before merge. Below: a severity-tagged summary, then the detailed backing. **For external users, gate on the two 🟠 HIGH items; everything 🟡/🟢 is deferrable and mostly deliberate.**

**🟠 HIGH — address before wider/external use**
- **No HTTPS/TLS in prod** (P0–1, security): `http://…:4096`, `COOKIE_SECURE=false` → credentials + session cookie in plaintext. Internal-only for now; flip to Caddy/HTTPS (`docker-compose.caddy.yml`, `SITE_ADDRESS`) + `COOKIE_SECURE=true` before real users.
- **Plan 6 not full-stack-verified** (P6, quality): live UI never clicked end-to-end — the "Plan 6 — Testing & Fix" section is the mitigation.

**🟡 MEDIUM — hardening / deferred-but-real**
- **CD not gated on CI + no branch protection on `main`** (P0): a direct push / red-CI merge deploys anyway. Add branch protection + gate CD on CI + a deploy-job `permissions:` block.
- **Flaky deploy SSH step** (P5): `:22 i/o timeout` seen once. Add retry/timeout + SHA-pin the marketplace actions (clears the Node-20 annotation).
- **Container not hardened** (P0): API runs as root; health check not DB-resilient. Add non-root `USER` + `/api/health/db` readiness.
- **Concurrency = last-write-wins** (P4–5): cargo `rowIndex` = `MAX+1` no-lock; `StatusService.fire` no row lock (§8.5). Fine single-user; needs row-lock/serializable+retry at real concurrency / the Stage-4 cascade.
- **Multi-tenant not enforced** (P2–6): `tenantId` present but no read path filters by it, uniques not composite `(tenantId,…)`. By design now; a real gap when tenancy activates.
- **MSDS file written outside the mediator tx** (P4): disk file + `FileAsset` row before the mediated link → orphan on post-store failure (low-prob).
- **`apps/web` bundle ~677 KB single chunk** (P6, perf, gzip ~201 KB): no route code-splitting. Lazy-load the wizard/route-builder later.

**🟢 LOW — by-design deferrals · cosmetics · test coverage · confirms**
- **Stage-4 reserved seams** (P3–5, by design): ChangeOrder cascade throws (unreachable), change-log is a no-op sink, `LegCargo.manifestSnapshot`/`Leg.totalChargeableWeight`/cargo FF-density stay null. Wired + waiting — not bugs.
- **Impact-class classifications to re-confirm vs §11.1** before the Stage-4 change-order fork (P4–5; e.g. `icaoCode`=Corrective vs `iata`/`unLocode`=RfqDefining).
- **Test-coverage gaps** (P5–6): "PATCH leg w/o `assignedCargoIds` preserves LegCargo", ICAO/phone/email validators, `revalidate(malformed-id)`, cross-query-404 (P5); PointEditor live type-switch interaction + Radix-Select test brittleness (P6). Logic verified in review; coverage thin.
- **Micro-efficiency** (P5–6): leg update pre-checks before the empty-patch short-circuit; `buildGraph` full-row fetch; `form.watch` whole-step re-renders. Cosmetic.
- **Prod smoke-test residue** (P5, data): leftover draft `YAL26-0001` + client `CL-0002` (no `DELETE /queries` endpoint exists — not in Plan 6 scope either). Wipe via Neon for a pristine sequence.
- **By-design UX** (P6): add-mode MSDS shows a "save row first" advisory (no cargo `cid` until saved); Contact-Person is a bare transient `<Select>`.
- **Config confirmations** (P1/P11): auth token TTLs (~15m/~7d), Neon↔droplet region match, the eventual `logistics.<domain>` subdomain + `:80/:443` edge pre-flight.

### Detailed backing

**Plan 6 follow-ups (Wizard UI — deployed; testing in progress)**
- **⚠ Full-stack verification pending** — the current top priority; see "Plan 6 — Testing & Fix" above.
- **Bundle code-split** — `apps/web` is a ~677 KB single chunk; lazy-load routes (`React.lazy` + `Suspense`).
- **jsdom/Radix test coverage** — PointEditor live type-switch (via the Select) untested (flaky; verified via a prop test); several tests drive Radix's hidden native `<select>` (brittle if Radix's DOM changes); `usePoints` update/remove invalidation now covered.
- **Tenant read-scoping** — `GET /queries` doesn't filter by `tenantId` (no read path does; see Multi-tenant below).
- **By-design UX** — add-mode MSDS advisory (no `cid` yet); bare Contact-Person select.
- **Reserved for Plan 7** — the wizard's Send Follow-up / Send Acknowledgement buttons were intentionally omitted.

**Plan 5 deferred minors** — Flat-findings dedup (✅ **resolved in Plan 6** via `dedupeFindings`); test-coverage breadth (leg-preserve-LegCargo, ICAO/phone/email validators, `revalidate(malformed-id)`, cross-query-404); micro-efficiency (leg `update` V-M1 + point re-fetch before short-circuit; `routing.buildGraph` full rows vs `select`); impact-class confirm (`icaoCode` Corrective vs `iataCode`/`unLocode` RfqDefining — re-confirm vs §11.1).

**Plan 4 tech-debt** — Atomic `rowIndex` hardening (`MAX+1` no lock → concurrent dup ordinal; harden with `@@unique([queryId, rowIndex])`+retry when concurrency matters); MSDS `FileAsset` outside the mediator tx (orphan on failure — move into the tx); impact-class re-validation vs §11.1 before the change-order fork.

**Extensibility deferred (Stage 4+)** — ChangeOrder cascade + durable change-log (`ChangeOrderStrategy` throws; `ChangeLog` no-op sink; `reopen` edge + cargo→leg fan-out built and waiting; `manifestSnapshot`/`totalChargeableWeight` null); `fire` concurrency (reads-then-appends + column write, no row lock — pair a row lock/serializable+retry with the Stage-4 cascade).

**Security / ops** — HTTPS + domain (HTTP-only `:4096`, `COOKIE_SECURE=false` → `docker-compose.caddy.yml` + flip `COOKIE_SECURE=true`); CI/CD hardening (retry/timeout on the deploy SSH step; SHA-pin actions; branch protection + gate CD on CI; deploy-job `permissions:`); container hardening (non-root `USER`; DB-resilient health + `/api/health/db`); multi-tenant (global uniques → composite `(tenantId,…)`; reads scope by tenant when tenancy activates); prod smoke-test residue (`YAL26-0001` + `CL-0002` — wipe via Neon if a pristine sequence is wanted).

**Done (for the record)** — Plans 0–6 shipped + deployed ✅ · Neon password rotated ✅ · CD compose mismatch + `--force-recreate` (PR #3) ✅ · reference-data auto-seed + stale-`:latest` fix (PR #10) ✅ · **Plan 5 (PR #12)** — points/legs/routing + `validateRoute` + route-gated Create Query, prod-verified ✅ · **Plan 6 (PR #13)** — Wizard & Query List UI + `GET /queries`, deployed + CI-green (373 tests); the opus whole-branch review caught+fixed a non-admin Step-1 403 (Critical) + Step-2 double-PATCH (Important); **manual/prod testing in progress**.

## Resume prompt for Plan 7 (start here once Plan 6 testing is green)
> "Read `Stage 3 - Create Query - Functional Spec.md` (§13 Send Follow-up / Send Acknowledgement, §14 Escalation & Notifications, §15 Email Templates), `Stage 3 - Technical Design.md` (§8.2 escalation scheduler, §8.3 notifications polling, §8.7 email compose-&-log, §2.2 modules), and `Stage 3 - Session Handoff.md`. Plans 0–6 are done — the whole Stage-3 API + the Create-Query wizard/list/route-builder UI (Plan 6 deployed; testing complete). Let's do **Plan 7 — Notifications / Escalations / Emails**. Scope to confirm in brainstorm: **(backend)** the `notifications` / `escalations` / `emails` modules — `EscalationsService` with the `@nestjs/schedule` cron (on first Save create 3 `Escalation` rows: +30m→Executive / +2h→Manager / +6h→Administrator; cron polls due+unfired+not-cancelled → writes `Notification` + composes an escalation email; cancel remaining on `RFQ_READY`/closure; **informational only** — no reassignment/auto-close, spec §14), `EmailsService` **compose-&-log** (`EmailLog`, no live send — D12; follow-up + acknowledgement templates §15 with tokens {Query_ID}/{Client_Name}/{Client_Email}/{Missing_Fields_List}/{Expected_Response_Timeline}), `NotificationsService` (in-app feed, FE-polled — `/notifications`, `/unread-count`, `PATCH /:id/read`, §8.3). **(frontend)** the notifications **bell + unread badge + feed** (in `AppLayout`), the wizard's **Send Follow-up** (enabled when ≥1 checklist item is unchecked → compiles the missing-fields list) + **Send Acknowledgement** buttons (Plan 6 intentionally OMITTED these), and an email-log view. New Prisma models `Notification` / `Escalation` / `EmailLog` (a migration). Consume Plan 6's query + checklist foundation. Scope boundary: change-order cascade + FF density + `manifestSnapshot` freeze stay **Stage 4+**. Reuse the `apps/web` + `apps/api` conventions (this handoff's "Conventions & key learnings") + the subagent-driven flow. Brainstorm → write the Plan 7 plan → execute subagent-driven on `feat/plan-7-notifications`."

---

## (Archived) Resume prompt for Plan 6
> "Read `Stage 3 - Create Query - Functional Spec.md`, `Stage 3 - Technical Design.md`, and `Stage 3 - Session Handoff.md`. Plans 0–5 are done, merged, deployed, and prod-verified — the whole Stage-3 API is built. Let's do **Plan 6 — Wizard & Query List UI** (frontend in `apps/web`, the client for the existing API). Build: (1) the **Query List (All Records)** page (`/queries`); (2) the **stepped Create-Query wizard** (`/queries/new`, `/queries/:id`) — `WizardShell` + stepper + per-step forms + a route-validation findings panel; steps (order O2): Client & Query · Shipment · Cargo (+ Export to Excel) · Legs / Route Builder · Notes & Checklist; save model = first Save (or first leg save) mints the Query ID, per-step PATCH, Create Query → `RFQ_READY`; (3) Step 4 — `LegEditor`/`PointEditor`/`CargoAssignmentControl`/`RouteDiagram`; (4) live client-side `validateRoute` from `@svyft/shared` + one findings component (draft warns / create blocks). Scope boundary: notifications/escalations/emails UI = Plan 7; change-order cascade + FF density + `manifestSnapshot` = Stage 4+. Brainstorm, write the plan, execute subagent-driven on `feat/plan-6-wizard-ui`." *(Delivered — PR #13.)*
