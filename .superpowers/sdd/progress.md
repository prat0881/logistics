# SDD Progress — Stage 3: Cargo Packing-List Re-model (Cargo→Package→Item)

Branch: `feat/stage-3-cargo-packing-list` (off `main` via docs branch; carries design+plan commits)
Design: `docs/superpowers/specs/2026-08-05-stage3-cargo-packing-list-design.md` (decisions C1–C14, open items O1–O5)
Plan:   `docs/superpowers/plans/2026-08-05-stage3-cargo-packing-list.md` (17 tasks)
Doc commits already on branch: b6dbfbe (design), 4a53135 (plan).
Task-1 BASE (feat tip before first impl): **4a53135**
Models: **haiku** for pure-transcription implementers (T1/T2 — full code in plan), **sonnet** for T3–T10 impl + task reviewers, **opus** for the final whole-branch review.

(Prior `.superpowers/sdd/progress.md` was the COMPLETED Stage-4 charge-config run — PR #50, merged; recorded in `docs/Stage 4 - Session Handoff.md`. Started fresh here.)

## SESSION SCOPE
- **THIS session: Tasks 1–10 (through the API).** End state: shared ✓ + api ✓ green on :5433; web intentionally RED (still on old flat-cargo API — Phase 4). Branch WIP, NOT merged.
- **NEXT session: Tasks 11–17 (web + docs) + final whole-branch review + finishing/PR.** Resume at first unchecked task below.

## ⚠ CRITICAL GIT PROTOCOL (carried from Stage-4 ledger — confirmed hazard on this SHARED checkout)
A background pull / another Claude session intermittently `git checkout main`s this shared checkout → a naive commit lands on `main`.
**PROTOCOL: implementers NEVER touch git** (no checkout/add/commit/stash) — they edit + test, then report exact files to stage + a suggested commit message. **The CONTROLLER commits atomically:** start EVERY commit with `git checkout feat/stage-3-cargo-packing-list`, then `git add <specific files>`, `git commit -m "..."`, then verify parent == previous feat tip (`git log --oneline -2`). Record each task's BASE (feat tip before dispatch) for review-package — never `HEAD~1`. Plan's per-task "Commit" steps are OVERRIDDEN by this protocol.

## ENV facts (verified this session)
- Dev Postgres UP: container `svyft-postgres-task4` on :5433. `apps/api/.env` present (DATABASE_URL/DIRECT_URL/JWT). Prisma CLI needs env exported: `set -a; . apps/api/.env; set +a`.
- **Migrations: hand-author `prisma/migrations/<ts>_<name>/migration.sql` + `pnpm --filter @svyft/api exec prisma migrate deploy --schema ../../prisma/schema.prisma` + `prisma generate`. NEVER `migrate dev`** (drifts on a generated col → PG 42601; may propose destructive RESET of the SHARED dev DB). If Prisma proposes RESET/DROP unexpectedly → STOP. Do NOT run `prisma format` (reformats unrelated models → churn).
- Shared compiled to `dist` (gitignored); **`pnpm --filter @svyft/shared build` after EVERY shared edit** or api/web/tsc see stale types. api jest maps `@svyft/shared`→src (no rebuild for api tests); tsc/web read dist.
- api has NO unit runner: tests in `apps/api/test/*.e2e-spec.ts` (Jest, `--runInBand`, real DB :5433). Full-AppModule e2e MUST `await app.close()` in afterAll (schedule cron keeps process alive). Prefer AppModule + `moduleRef.get(PrismaService)`.
- CI has no seed: e2e needing catalogue rows call `seedReferenceData(prisma)` in beforeAll. Self-cleaning by unique prefix.
- vitest (shared+web) does NOT type-check → run `pnpm --filter @svyft/shared typecheck` + `npx tsc --noEmit` on new shared test files; web typecheck separate (`pnpm --filter @svyft/web typecheck`). Lint = `pnpm run lint`.
- RBAC: cargo/package/item = workflow writes → Executive+ (authenticated, NO `@Roles`).

## PRE-FLIGHT plan scan — conflicts fixed before execution
- **T3 migration mechanism (FIX):** plan says `prisma migrate dev` → WRONG here. Rewrite T3 to hand-authored SQL + `migrate deploy`. AND switch strategy from in-place RENAME to **DROP + CREATE the cargo cluster** (user-approved), preceded by a **one-time confirmed TRUNCATE of transactional tables** (queries + downstream; keep masters/config/catalogue). Removes rename/generated-col fragility + FK-orphan risk. **Rewrite T3 in the plan file BEFORE dispatching its implementer; confirm the exact TRUNCATE statement with the user before running it.**
- **Commit steps:** overridden by the git protocol above (controller commits).
- **Ripple (heads-up, not a blocker):** the re-model breaks extensive merged Stage-4 cargo references (QuoteCargoLine, quote engine, manifest/charge snapshots, leg assignment, many e2e/web specs). Implementers fix breakages as they surface within each task's scope; effective scope > 17 tasks. Web breakage is expected and deferred to Phase 4 (next session).

## Tasks (dep order — Phase 1 shared → 2 prisma → 3 api ‖ THIS SESSION ‖ then 4 web → 5 docs NEXT SESSION)
- [x] T1 shared: PackageType/UnitOfMeasure enums, DG tag, TONNE unit, canonical helpers ✅ c078b2b
- [x] T2 shared: Cargo/Package/Item schemas + DTOs + effectiveTags + per-package DG create-finding ✅ 0a90965
- [x] T3 prisma: Cargo/Package/Item + LegPackage DROP+CREATE migration + model e2e ✅ 0768b2d
- [x] T4 api: Cargo grouping CRUD + derived header DTO ✅ 285b7fd
- [x] T5 api: Package CRUD — canonical storage, packageNo V-5, MSDS ✅ 3aff37b
- [x] T6 api: add-N-copies package clone ✅ 67ff641
- [x] T7 api: Item CRUD + V-2/V-4 partial-PATCH fix + T6 copy() rollback ✅ 9c09116
- [ ] T8 api: query.dgIndicator from DG tag union (sonnet)
- [ ] T9 api: Excel export restructure — per-item packing list (sonnet)
- [ ] T10 api: leg mapping at package grain (LegPackage) + canonical leg roll-ups (sonnet)
- [ ] T11 web: data hooks cargo/package/item (NEXT SESSION)
- [ ] T12 web: main table + two-level expansion (NEXT SESSION)
- [ ] T13 web: cargo popup shell + nested save (NEXT SESSION)
- [ ] T14 web: package editor (canonical CBM, packageType, MSDS, copies) (NEXT SESSION)
- [ ] T15 web: items mini-table (V-4, DG) (NEXT SESSION)
- [ ] T16 web: leg assignment control at package grain (NEXT SESSION)
- [ ] T17 docs: align Functional Spec + Technical Design + Handoff (NEXT SESSION)

---

## Ledger
(one line appended per task as each review comes back clean)

**Pre-exec:** docs-align commit `6a01c55` sits on top of T1 — design §3.6/§11 switched to DROP+CREATE + one-time transactional wipe + `migrate deploy`; plan Global Constraints add migrate-deploy + controller-commit protocol; Task-3 got a REVISED note (supersedes its in-place-rename SQL). **T3 implementer must follow the DROP+CREATE approach, not the stale Step-4 SQL.**
**T1 ✅ COMPLETE (commit `c078b2b`, parent 4a53135, review Approved).** cargo.ts: PackageType/UnitOfMeasure const-enums, DG tag, TONNE (order KG→TONNE→GM), canonical helpers (toCanonicalDim/fromCanonicalDim, toCanonicalWeight/fromCanonicalWeight, cbmFromCanonical=L·W·H/1e6), label maps. Stale WEIGHT_UNITS/REFERENCE_TAGS `toEqual` pins updated in place. shared 255/255. Reviewer (sonnet) hand-verified every formula + ordering; ✅ Approved, no Critical/Important in T1's own two files. **Current tip `6a01c55`; T2 BASE = 6a01c55.**

⚠ CARRY-FORWARD RIPPLE / FINDINGS (from T1 review — none block T1; owned by later tasks):
- **[T4] apps/api typecheck break (plan-sequenced):** `apps/api/src/modules/cargo/cargo.service.ts:61,72` assign widened shared `ReferenceTag`/`WeightUnit` into Prisma's still-narrow enums → `tsc` fails once `@svyft/shared` is rebuilt. Resolved when T3 drops CargoItem + T4 rewrites cargo.service.ts. Do NOT gate apps/api standalone until the API phase is done. (api jest = esbuild → e2e still RUN; only `pnpm --filter @svyft/api typecheck` is red until T4.)
- **[PHASE 4 / T12] apps/web unowned break:** `apps/web/src/components/ReferenceTagIcons.tsx:5` `Record<ReferenceTag,…>` missing `DG` → web tsc fails after shared build. Component also has a separate `isDangerous` prop to reconcile to the DG tag. No task owns it → FOLD INTO Phase-4 (T12/T15 ReferenceTagIcons rework: drop isDangerous, handle DG as a tag). Web is intentionally red this session.
- **[T10 + manifest.ts] toKg TONNE gap:** pre-existing `toKg` (cargo.ts) ignores TONNE (→1000× undercount). Not live yet (Prisma enum KG/GM until T3). Call sites `queries.service.ts:334-335` (T10 → pure-Σ canonical) + `rfq/manifest.ts:38,40` (reads cargo weightUnit; breaks at T3/T5, migrate off per-row unit). Confirm both migrated before TONNE is selectable; toKg likely deletable.
- **[MINOR/triage] label test coverage:** `packageTypeLabel`/`uomLabel` have no pin tests (brief gap). Add in T2 or final triage.
- **Gate reminder:** whenever `packages/shared` changes, run `pnpm --filter @svyft/shared build && pnpm -r typecheck` (vitest/esbuild don't typecheck).

**T3 PREP (DB verified via container psql on :5433):** ALL transactional tables are EMPTY (Query/CargoItem/LegCargo/QuoteCargoLine/Leg = 0) → **NO TRUNCATE needed** — the "one-time transactional wipe" is a no-op; skip it, no user confirmation required. Clean DROP+CREATE on empty tables. FKs referencing `CargoItem` = EXACTLY `LegCargo_cargoItemId_fkey` (onDelete CASCADE) + `QuoteCargoLine_cargoItemId_fkey` (onDelete RESTRICT) — no others. Migration order: CREATE enums (`PackageType`,`UnitOfMeasure`; `ALTER TYPE "ReferenceTag" ADD VALUE 'DG'`; `ALTER TYPE "WeightUnit" ADD VALUE 'TONNE' AFTER 'KG'`) → `DROP TABLE "LegCargo"` → `ALTER TABLE "QuoteCargoLine" DROP CONSTRAINT "QuoteCargoLine_cargoItemId_fkey"`, rename `cargoItemId→packageId` → `DROP TABLE "CargoItem"` → CREATE `Cargo`/`Package`(generated volumeCbm + `@@unique(queryId,packageNo)`)/`Item`/`LegPackage` → ADD `QuoteCargoLine.packageId` FK→`Package`. Apply: `set -a; . apps/api/.env; set +a` then `pnpm --filter @svyft/api exec prisma migrate deploy --schema ../../prisma/schema.prisma` + `prisma generate`. Container psql for verification: `docker exec -e PGPASSWORD=$DBPASS svyft-postgres-task4 psql -U $DBUSER -d svyft -Atc "…"` (parse DBUSER/DBPASS from DATABASE_URL). Masters/config kept.

**T2 ✅ COMPLETE (commit `0a90965`, parent 6a01c55, review Approved).** cargo.ts: `cargoCreateSchema` (grouping) + `packageCreateSchema` (V-2 net≤gross) + `itemCreateSchema` (V-4 qty⇒uom) + update variants + `CargoDto`/`PackageDto`/`ItemDto` + `effectiveTags` (order-stable dedup). query.ts: `PackageForValidation` + package-grain `collectCreateFindings` (F6 DG→MSDS, scope type `package`). findings.ts: `FindingScope.type += "package"` (additive; `findingTabKey` routes F6 via rule-OR — reviewer-verified). Stale flat-schema tests fully reconciled onto package/item schemas (reviewer read un-diffed tails; none dropped). shared 258/258, typecheck/build/lint clean. Reviewer (sonnet): ✅ Approved, no Critical/Important. Minor heads-up: `PackageForValidation.dimL/W/H/grossWt` unread by F6 (spec-mandated; dims/gross guaranteed by packageCreateSchema V-1) — for Task 4+. **T3 BASE = 0a90965.**

=== ✅ PHASE 1 COMPLETE (T1–T2): shared contracts done. `4a53135..0a90965` (+ docs 6a01c55). shared 258/258, typecheck/build/lint clean. → Phase 2 (Prisma migration, DROP+CREATE). ===

⏸→▶ **T3 paused ("pause for Task 3"), then user confirmed: RESUME T3 ON LOCAL.** (First dispatch touched nothing — verified: clean tree, DB unchanged.) User asked "will T3 run on Neon prod?" → answered: NOT this session (local :5433 only); Neon prod gets it AUTO on merge-to-main via CD `prisma migrate deploy` (Technical Design §11.6) — destructive (DROP CargoItem/LegCargo), pre-go-live/data-disposable.
- **PROD-SAFETY added to the T3 migration:** `DELETE FROM "QuoteCargoLine";` before the DROP/repoint, so the final `ADD CONSTRAINT QuoteCargoLine_packageId_fkey` cannot orphan even on a non-empty Neon prod. (Local QuoteCargoLine already empty; harmless there.)
- 🔴 **GO-LIVE GATE (for finish/PR — NEXT SESSION):** BEFORE this branch merges to main, VERIFY Neon prod `CargoItem`/`LegCargo`/`QuoteCargoLine` are empty (or explicitly accept the pre-go-live wipe). DB migrations do NOT auto-rollback with the app image. Add this to the PR body + go-live checklist. Neon creds are NOT in this checkout (droplet .env / GH secrets).
T3 BASE `0a90965`; DROP+CREATE SQL + FK facts in the T3 PREP note above.

**T3 ✅ COMMITTED `0768b2d` (parent 0a90965) — migration APPLIED to LOCAL :5433; DB independently verified by controller (Cargo/Package/Item/LegPackage exist, CargoItem/LegCargo gone, WeightUnit=KG,TONNE,GM, ReferenceTag ends …,OUT_OF_GAUGE,DG, volumeCbm expr=`((("dimL"*"dimW")*"dimH")/(1000000)::numeric)`, QuoteCargoLine.packageId). Review IN FLIGHT (opus).** Model test GREEN via isolated-transpile (real AppModule boot + real DB). Evidence-driven deviations: corrected volumeCbm dbgenerated byte-match; `tags @default([])` on Package/Item (Prisma Client needs it); proactive QuoteCargoLine unique-index rename; `DELETE FROM "QuoteCargoLine"` prod-safety line. Residual `migrate diff` = ONLY the pre-existing repo-wide id-default convention (schema `@default(uuid())` + DB `gen_random_uuid()`; MessageLog/ScheduledEvent identical) — accepted. **T4 BASE = 0768b2d.**

🔧 **API-PHASE WORKFLOW REALITY (critical for T4–T10):**
- api jest = **ts-jest with WHOLE-GRAPH TYPECHECK** (no isolatedModules; `test/jest-e2e.json` maps `@svyft/shared`→src). Until apps/api fully compiles, NO e2e runs via standard `pnpm --filter @svyft/api test`. → **each API-task implementer runs its test via an ISOLATED-TRANSPILE jest config** (scratchpad copy of `test/jest-e2e.json` with ts-jest `isolatedModules: true`; NOT committed) — boots real AppModule + real DB, skips unrelated broken files' type errors. Rebuild shared (`pnpm --filter @svyft/shared build`) after any shared edit before api TYPECHECK.
- 🔴 **API RIPPLE > nominal T4–T10.** Files referencing dropped CargoItem/LegCargo/old fields that MUST be fixed for the api to compile: **9 src** — cargo/cargo.impact.ts, cargo/cargo.service.ts, ff-portal/ff-portal.service.ts, legs/legs.service.ts, queries/queries.service.ts, rfq/leg-context.ts, rfq/manifest.ts, rfq/rfq.service.ts, routing/routing.service.ts — **+ packages/shared/src/legs.ts + ~27 existing e2e specs**. Planned tasks own SOME (T4 cargo*, T8 queries.service, T10 legs.service+shared legs). UNOWNED ripple: rfq/* (leg-context, manifest, rfq.service), ff-portal.service, routing.service, + the 27 Stage-4 specs (rfq-distribute/quote-pricing/ff-portal/change-order/routing/legs — build cargo via prisma.cargoItem or the old cargo API). → **Needs a ripple-cleanup pass (fold into T10 or add T10b) + update/rewrite/remove broken specs. Likely EXTENDS the API phase beyond one session.**
- Final API gate (after ripple fixed): `pnpm --filter @svyft/shared build && pnpm --filter @svyft/api typecheck && pnpm --filter @svyft/api test` all green.

**T3 ✅ REVIEW APPROVED (opus).** Independently DB-verified: volumeCbm byte-identical (schema↔pg_get_expr), checksum matches _prisma_migrations, DROP ordering clean (0 FKs into CargoItem → no CASCADE), enum ADD VALUE gotcha avoided, schema↔migration consistent. No Critical/Important. Minors (→final triage): id-default drift (pre-existing convention); model-test title says "cascades cargo→package→item" but Package deletes via the Query→Package edge (add a `prisma.cargo.count` assertion to isolate Cargo→Package); V-5 test uses generic `rejects.toThrow` (could assert P2002). ⚠ Neon-prod constraint names verified transitively (identical migration history) — CD `migrate deploy` is final confirmation (see go-live gate).

=== ✅ PHASE 2 COMPLETE (T3): Prisma re-model migrated to local :5433, opus-approved. `0a90965..0768b2d`. → Phase 3 (API: T4–T10 + ripple cleanup). **T4 BASE = 0768b2d.** ===

**T4 COMMITTED `285b7fd` (parent 0768b2d) — REVIEW IN FLIGHT (sonnet).** Cargo grouping CRUD + `shapeCargo` derived header (H4–H8); new `cargo/cargo-shape.ts` (shapePackage/shapeItem SKELETONS → completed in T5/T7); rewrote cargo.impact/module/controller (auth-only, param validation, mediated). cargo.e2e-spec repurposed, **6/6 via the isolated ts-jest config** (obsolete flat/MSDS/export tests removed → return in T5/T7/T9). **⚠ OUT-OF-SCOPE STOPGAP committed with T4:** `routing/routing.service.ts` `buildGraph`/`legsCarryingCargo` stubbed to EMPTY — they referenced dropped `prisma.cargoItem`/`legCargo` and were 500-ing EVERY mediated write app-wide (reproduced via points.e2e-spec). Cargo route-validation (R1–R9) is now QUIET. **→ T10 (or ripple cleanup) MUST re-derive routing.service.ts route graph from LegPackage/Package to restore R1–R9** — add to T10 scope; routing.service.ts is thus PARTIALLY handled (stopgap only), not done. **T5 BASE = 285b7fd.**

**T4 ✅ REVIEW APPROVED (sonnet).** Cargo grouping CRUD/shapeCargo/impact/controller/test spec-correct + genuinely tested (reviewer field-checked all DTOs, verified auth-only global guard, non-tautological tests, cascade-backed cleanup). Findings:
- 🔴 **IMPORTANT → T10 MUST FIX:** the T4 routing.service.ts stopgap (`legsCarryingCargo → []`) doesn't just quiet route findings — it **defeats change-order gating for Structural cargo changes**. legsCarryingCargo=[] → `ImpactClassifier` self-scopes to `{type:"cargo"}` → `ScopeResolver.downstreamWork` only inspects `type==="leg"` → always false → `decidePath` free-paths even Structural `@create/@delete`. INERT today (nothing can populate Package/LegPackage yet), but once packages ride legs with a live RFQ, deleting a Cargo would silently skip change-order review. **T10 (routing re-derivation) MUST restore `legsCarryingCargo` from LegPackage/Package for BOTH (a) R1–R9 route findings AND (b) ScopeResolver leg-scoping / change-order gating** — correctness gate before any RFQ/quote work touches cargo.
- Minor: R3/C1 route rules go NOISY false-positive (every leg "carries no cargo", blocking at phase=create), not quiet — fixed by the T10 re-derivation. Minor: cargo-shape.ts tag-cast inconsistency (align in T5/T7).

⚠️ **GIT DRIFT FIRED during the T4 review** — HEAD jumped to `main` (497e0d2) mid-review (background process, exactly the ledger-warned hazard). Reviewer re-verified via `git show 285b7fd:<path>`; verdict stands. Controller restored `feat/...`@285b7fd (all commits safe on the branch ref; ledger is gitignored so it survived). **HAZARD IS ACTIVE — real risk for T5–T10: a mid-task drift makes an implementer edit main's files (which lack T1–T4 + the migration). Mitigation needed before continuing long implementer runs.**

=== ✅ T4 COMPLETE — Cargo grouping API done. `0768b2d..285b7fd`. → T5 (Package service). ===

**Post-T4 correction commit `d7326d3` (parent 285b7fd):** routing.service.ts stopgap comments corrected (R3/C1 go NOISY not quiet; `legsCarryingCargo` DEFEATS change-order gating) per T4 review Important#1/Minor#1 — comment-only, no behavior change. **T5 BASE = d7326d3.**

🟢 **SESSION CHECKPOINT (user paused the drift source):** integrity swept — feat has exactly 8 commits on top of `main@497e0d2` (b6dbfbe design · 4a53135 plan · c078b2b T1 · 6a01c55 docs · 0a90965 T2 · 0768b2d T3 · 285b7fd T4 · d7326d3 routing-comment-fix); `main` untouched (no stray commits); tree clean; DB re-model intact. Untracked `CLAUDE.md` + `docs/README.md` = the OTHER session's artifacts (appeared during drift) — left untouched, never staged. Drift source paused → T5–T10 safe to resume; still checkout-first every commit. **Resume point: T5 (Package CRUD — canonical storage, packageNo V-5, MSDS), BASE d7326d3.**

**T5 COMMITTED `3aff37b` (parent d7326d3) — REVIEW IN FLIGHT (sonnet).** Package CRUD (canonical cm/kg via parent-cargo units; packageNo V-5 per-query case-insensitive 409; MSDS via FilesService, mediated) + `shapePackage` fleshed (canonical strings, effectiveTags, items, tag-cast fixed) + `packageImpactMap`. package.e2e-spec 8/8 (14/14 with cargo) via isolated config. **T6 BASE (pending review) = 3aff37b.**
- 🟠 **KNOWN GAP → T7 MUST FIX (both Package V-2 + Item V-4):** partial PATCH bypasses the Zod refine — `packageUpdateSchema`/`itemUpdateSchema` `.refine()` see only the patch, so PATCH netWt-alone can violate the stored grossWt (V-2), and PATCH qty-alone mis-validates V-4. Fix = **service-level merge (existing row + patch) then validate**, in `package.service.update` AND `item.service.update`. (Implementer also spawned a user-visible chip `task_36b79bbe`.)
- Notes: API-side packageNo auto-suggest intentionally NOT built (UI-layer → T14); create bad-cargo-ref=400, update/remove missing=404 (intentional).

**T5 ✅ REVIEW APPROVED (sonnet — thorough: live HTTP round-trip + direct schema tests + tsc/grep).** All binding constraints verified: canonical conversion on create+update (update re-loads the cargo's units), `volumeCbm` DB-generated, V-5 query-scoped/case-insensitive/409, exact impact map, mediation, `shapePackage` complete, auth/param validation, clean scope (no item/copies/dgIndicator). No Critical. **IMPORTANT (deferred → T7): V-2 partial-PATCH gap CONFIRMED LIVE** — a normal single-field PATCH (e.g. `{grossWt:50}` on a stored 100/90 row) returns 200 and persists `netWt>grossWt`; create is safe (grossWt required). Fix in T7 = service-level merge-then-validate in `package.service.update` AND `item.service.update`. Minors (no action): loose `data` cast (pre-existing idiom); V-5 DB backstop case-sensitive (by-design).

=== ✅ T5 COMPLETE — Package CRUD API done. `d7326d3..3aff37b`. → T6 (add-N-copies). **T6 BASE = 3aff37b.** ===
=== 📊 SESSION SO FAR: T1–T5 done + reviewed (shared×2, migration, cargo API, package API) — 9 commits on feat over `main@497e0d2`. REMAINING API: T6 copies · T7 item (+V-2/V-4 fix) · T8 dgIndicator · T9 export · T10 legs + routing re-derivation (restore R1–R9 + change-order gating) · +~27 Stage-4 spec rewrites + rfq/ff-portal/queries ripple. Then Phase 4 web (T11–16) + Phase 5 docs (T17). ===

- [x] T6 api: add-N-copies package clone ✅ 67ff641
**T6 COMMITTED `67ff641` (parent 3aff37b) — ✅ REVIEW APPROVED (sonnet).** `PackageService.copy` (2..50 clones, fresh per-query packageNos, deep-copied items, shared msdsFileId, per-clone mediated @create). package.e2e-spec 14/14 via isolated config. Concerns (non-blocking, disclosed): non-atomic batch (mediator has no external-tx hook — matches queries.service.createQuery pattern; partial-failure leaves some clones); `:cid` not cross-checked (pid unique + queryId-scoped, redundant); packageCount/msdsFileId by-design. Reviewer verified all binding constraints + traced packageNo skip-taken edge case. **IMPORTANT (small tracked fix, non-blocking): `copy()` non-atomic batch has NO partial-failure rollback** — mid-batch error leaves an unsignaled prefix of clones (createQuery precedent doesn't transfer: it's idempotent-by-retry, copy isn't). Fix = try/catch + best-effort `prisma.package.deleteMany({id:{in:createdIds}})` before re-throw, inside `PackageService.copy`. Minors: `:cid` not cross-checked (harmless, clone inherits source.cargoId); generated packageNo not re-checked vs 60-char cap (low likelihood); one supertest socket flake (transport, not logic).

=== ✅ T6 COMPLETE — add-N-copies done. `3aff37b..67ff641`. → T7 (Item CRUD + V-2/V-4 partial-PATCH fix). **T7 BASE = 67ff641.** ⏳ PENDING SMALL FIX before PR: T6 `copy()` rollback-on-partial-failure (Important, non-blocking) → ✅ **RESOLVED in T7 §B (commit `369e89f`).** ===

**T7 COMMITTED — TWO commits over BASE `67ff641`: `369e89f` (carry-forward fixes) + `9c09116` (Item CRUD). ✅ REVIEW APPROVED (sonnet, deep independent verification).** Three parts, all landed:
- **§A1 Package V-2 partial-PATCH fix** (`package.service.ts` `update`): captures the loaded `existing` row, computes EFFECTIVE canonical gross/net by merging `data` (patched→canonical) over stored, throws `BadRequestException` if `effNet > effGross`. `packageUpdateSchema.refine` KEPT (lenient — never false-rejects; early both-present catch). Reviewer hand-traced grossWt-only/netWt-only/netWt→null: 400/400/200 all correct.
- **§A2 Item V-4 partial-PATCH fix**: REMOVED the `.refine()` from `itemUpdateSchema` in `packages/shared/src/cargo.ts` (a partial patch can't see stored uom → old refine false-rejected `{qty}` alone); `itemCreateSchema.refine` untouched. V-4 now enforced in `item.service.update` via merge-then-validate (effQty/effUom from patch-or-stored). **Deliberate asymmetry (item refine removed, package refine kept) — reviewer confirmed correct & no other consumer under-validated** (`itemUpdateSchema`'s only real consumer = item.controller; dist rebuilt & verified).
- **§B `copy()` rollback**: `createdIds` tracks only truly-committed clones (pushed after `mediator.apply` resolves w/ `needsConfirmation=false`); `try/catch` best-effort `prisma.package.deleteMany({id:{in:createdIds}})` (items cascade) before re-throw; cleanup errors swallowed, original never masked. Reviewer verified via `free-path.strategy.ts` that "pushed"=="committed", and traced the test's findFirst-call-count mock.
- **§C Item service** (`item.service.ts`/`item.controller.ts`/`item.impact.ts` new; `cargo.module.ts` registers): faithful mirror of Package pattern — param-level Zod pipe, mediated create/update/remove (entity `"item"`, all `Corrective`), `assertPackageRef` P2003 pre-verification, rowIndex race, auth-only; imports existing `shapeItem`; empty-item guard on create; returns `ItemDto`. Reviewer checked `impact.classifier.ts`/`scope.resolver.ts`: `"item"` needs no special plumbing (self-scope default, like `package`).
- **Tests (isolated ts-jest config, real AppModule + DB :5433):** item 10/10, package 16/16 (26/26), shared 259/259 (cargo.test.ts 27/27), shared typecheck clean, eslint clean on all 9 files. Prettier-drift on 4 modified files was PRE-EXISTING (reviewer re-verified against base 67ff641) — new T7 files 100% clean.
- 🟢 **NO GIT DRIFT this run** (implementer confirmed HEAD stayed `67ff641`; controller committed checkout-first, verified `HEAD~2 == 67ff641`).

⚠ **T7 MINOR findings → carry to FINAL whole-branch review triage (NOT blocking; none are correctness bugs):**
- **[Minor, coverage]** Package V-2 regression tests only the grossWt-only shape (`package.e2e-spec.ts:185-202`); netWt-only + netWt→null shapes hand-verified correct but untested. Matches context §A3's literal mandate (package = 1 case; item = 3 cases, all tested). Optional belt-and-suspenders add.
- **[Minor, DRY]** `ConflictException`-on-`needsConfirmation` 6-line block now duplicated 3× more in item.service (7 total across package+item). Inherited/mandated-mirror pattern — candidate for a shared `assertApplied(result)` helper in a cross-cutting cleanup, not a T7 fix.
- **[Minor, evidence]** No scoped `tsc` proof for the 3 new item.* files alone (whole apps/api typecheck expected-RED until T8–T10). Covered by 26/26 runtime tests + reviewer's manual key-by-key `itemImpactMap` cross-check. Closes automatically once T10 unblocks `pnpm --filter @svyft/api typecheck`.

=== ✅ T7 COMPLETE — Item CRUD + V-2/V-4 partial-PATCH fix + copy() rollback. `67ff641..9c09116` (2 commits). → T8 (query.dgIndicator from DG tag union). **T8 BASE = 9c09116.** ===

🔀 **API-PHASE RE-SCOPE (post-T7 discovery — the ledger-predicted ripple is now concrete).** `queries.service.ts` is broken far beyond T8's `syncDgIndicator`: the query GRAPH (`cargo` include — old `CargoItem[]` relation, now `cargos Cargo[]`), `shapeQuery` leg roll-ups (T10 read-side: `toKg`, `assignedCargoIds`, `c.qty`/`weightUnit`), AND `createQuery` F6 findings (`q.cargo`/`isDangerous`) all reference the dropped model. Every query create/read hits `syncDgIndicator`+graph+`shapeQuery`, so the plan's T8-only / T10-only file split is impossible. The plan's T8/T9/T10 are re-decomposed into coupled, independently-testable units (ledger foresaw: "fold ripple into T10 / extends beyond one session"):
- **Unit 1 = T8 + query-read re-model** (dispatch first; brief `unit1-brief.md`): `queries.service.ts` full re-model (graph `cargo`→`cargos`+nested pkgs/items & `legCargo`→`legPackages`; `shapeQuery` returns `shapeCargo`'d `cargos` + leg roll-ups as canonical Σ over assigned `Package` [drop `toKg`; `totalPackages`=count, Package has no qty]; `syncDgIndicator`=`package|item` DG-tag union; `createQuery` field-findings at package grain) + wire `syncDgIndicator` into `PackageService`/`ItemService` create/update(/copy) via injected `QueriesService` (CargoModule imports QueriesModule — **NO cycle**: only app.module imports CargoModule; QueriesModule already `exports:[QueriesService]`). Test = NEW spec (legacy `queries.e2e-spec.ts` is partly broken → deferred). Does NOT touch legs.service (leg WRITE) — roll-ups read empty until 1b.
- **Unit 1b = T10-legs**: `legs.service.ts`+`leg.impact.ts`+shared `legs.ts` (`assignedCargoIds`→`assignedPackageIds`, `LegCargo`→`LegPackage`, `assertPackageRefs`). Test `legs.e2e-spec.ts` (assign pkgs → roll-ups populate). Depends on Unit 1.
- **Unit 2 = T10-routing**: `routing.service.ts` re-derive route graph from `LegPackage`/`Package` → **restore R1–R9 route findings AND change-order leg-scoping/gating** (undo the T4 `legsCarryingCargo→[]` stopgap). Test `routing.e2e-spec.ts`. Depends on 1b.
- **Unit 3 = T9**: `cargo.service.ts` `exportXlsx` → per-item packing list.
- **Unit 4 = ripple/specs**: rfq/* (leg-context, manifest, rfq.service), ff-portal.service, config-data.service, impact.classifier, seed + ~27 legacy Stage-4 e2e specs → full `apps/api` compile + green e2e. The big tail; likely spills to next session.
Key schema facts: Query→`cargos Cargo[]`+`packages Package[]`; Package canonical grossWt/netWt/volumeCbm, **no qty/weightUnit**; `Leg.legPackages LegPackage[]` exists; `LegPackage(legId,packageId)`.

**✅ UNIT 1 COMPLETE — commit `d3fc12c` (parent 9c09116). REVIEW APPROVED (sonnet, deep independent verification).** queries.service.ts fully re-modelled: `QUERY_GRAPH_ARGS` cargo→cargos(+packages+items)/legCargo→legPackages; `shapeQuery` returns `shapeCargo`'d `cargos` + per-leg canonical-Σ roll-ups (`totalPackages`=count, `toKg` GONE incl. import, `assignedPackageIds`); `syncDgIndicator`=package|item DG-tag union (short-circuit, only-ever-sets-true); `createQuery` field-findings at package grain (PackageForValidation[] w/ effectiveTags). dgIndicator wired into PackageService create/update/copy + ItemService create/update (inside mediator tx; NOT delete/attachMsds), CargoModule imports QueriesModule (reviewer re-traced transitive import graph → NO cycle). Test `query-tree.e2e-spec.ts` 6/6 (isolated cfg); reviewer re-ran prettier/eslint on all 5 files (clean) + verified FreePathStrategy tx-wrapping + PackageForValidation shape match. NO git drift (HEAD stayed 9c09116 through impl; committed checkout-first, HEAD~1==9c09116).
⚠ **Unit-1 MINORs → FINAL-review triage (non-blocking):**
- **[Minor, commit hygiene]** `prettier --write` folded PRE-EXISTING >100-char drift cleanup into this feature commit (queries.service.ts + package.service.ts) — reviewer confirmed pure line-wrap, no behavior change, files now prettier+eslint clean. **DECISION: keep (compliant is the repo standard) + run a whole-branch `prettier --write` normalization at the FINAL GATE** so T7-reverted lines + these converge consistently. (Contrast: T7 reverted its prettier churn → those files still carry drift; normalize at end.)
- **[Minor, coverage]** `createQuery` package-grain re-model has 1 test (bonus, not RED-first); no explicit pass-case test (DG pkg + MSDS → no F6) or item-only-DG-no-MSDS case. Type-verified correct. Add breadth when the ~27 legacy specs are rewritten (Unit 4).

=== ✅ UNIT 1 COMPLETE — query create/read re-model + dgIndicator. `9c09116..d3fc12c`. → Unit 1b (T10-legs: legs.service+leg.impact+shared legs.ts at package grain). **Unit 1b BASE = d3fc12c.** ===

🟡 **CONCURRENT-SESSION COMMIT (benign, NOT the destructive drift):** while Unit 1b's implementer ran, another session (`Pratik Jain <pratik@svyft.ai>`) committed `29294fb docs(stage-3): session handoff — …paused after T6/17` on top of `d3fc12c` — touches ONLY `docs/Stage 3 - Session Handoff.md` (+6). Linear, non-destructive; `d3fc12c` stays in history. **That commit's "paused after T6" is STALE (another session's view) — THIS `progress.md` is the authoritative record (we're at Unit 1b, past T7+Unit 1).** A concurrent session is active on this shared branch → keep checking HEAD before every commit (protocol already does).

**✅ UNIT 1b COMPLETE — commit `97cf551` (parent `29294fb` → `d3fc12c`; linear). REVIEW APPROVED (sonnet, grep-verified rename + traced shapeQuery for the roll-up test).** Clean mechanical rename of the leg↔cargo join → leg↔package: shared `legSaveSchema.assignedCargoIds`→`assignedPackageIds` (+legs.test.ts reconciled), `leg.impact.ts` key (stays Structural), `legs.service.ts` (`load` include, `assertCargoRefs`→`assertPackageRefs`=prisma.package.count, create/update `LegCargo`→`LegPackage`/`packageId`, remove comment). legs.e2e-spec rebuilt onto Cargo+2×Package + a write→read roll-up test (assign 2 pkgs → GET /queries → `assignedPackageIds` + `rollup.totalPackages`=2 + `totalGrossWt`≈15 canonical). Tests: legs 5/5 (RED-first), shared 259/259 (legs.test.ts reconciled), shared typecheck clean. No prettier churn (symmetric numstats). Reviewer confirmed no behaviour change (V-M1/F8/mediation/legCode untouched), LegPackage schema shape matches, zero leftover old-model refs, no dangling apps/api caller.
⚠ **Unit-1b MINORs → tracked (both PRE-EXISTING, out of 1b scope):**
- **[Minor→ owns Unit 1 read-side / fold into web-DTO or Unit 4] `packages/shared/src/query.ts:356` `QueryLegDto.assignedCargoIds: string[]` is STALE** — `shapeQuery` already emits `assignedPackageIds` (runtime≠type). Inert for API (no apps/api consumer), but a latent trap for Phase-4 web (which reads `.assignedCargoIds`). Fix = rename the DTO field → `assignedPackageIds` when the web/DTO layer is touched (Phase 4) or in Unit-4 cleanup. Rebuild shared after.
- **[Minor, trivial] `apps/api/src/modules/cargo/package.service.ts:43`** comment says "mirrors …assertCargoRefs" — now `assertPackageRefs`. Fix in a simplify/comment pass.

=== ✅ UNIT 1b COMPLETE — legs at package grain. `d3fc12c..97cf551` (excl. concurrent docs `29294fb`). → Unit 2 (T10-routing: routing.service.ts re-derive route graph from LegPackage/Package → restore R1–R9 + change-order gating). **Unit 2 BASE = 97cf551.** ===

**✅ UNIT 2 COMPLETE — commit `ddf75ec` (parent `97cf551`). REVIEW APPROVED (sonnet — traced the gating end-to-end, zero findings).** 🔓 **The change-order-gating correctness gate (flagged since T4) is RESTORED.** routing.service.ts: `buildGraph` re-derives `cargo`=one RouteCargo per Package (effectiveTags→isDangerous, msdsFileId, grossWt/volumeCbm) + `legCargo`=LegPackage rows (packageId in the legacy cargoItemId slot — shared RouteGraph/validateRoute untouched, grain-agnostic); `legsCarryingCargo` re-derived (cargo→packages→legs, deduped); NEW `legsCarryingPackage`. impact.classifier.ts: `case "package"`→`legsCarryingPackage` (restores leg-scoping so ScopeResolver.downstreamWork can see downstream for RfqDefining/Structural package changes); NO `case "item"` (all Corrective → never forks). routing.e2e 4/4 (valid-route/R5/R9/fan-out) + broader 54-test regression clean. Reviewer independently verified: opaque-id join sound, `ChangeMediator.apply` runs `classify()` BEFORE the delete `uow` (scope resolves against live LegPackage rows — fix is LIVE not inert), C3 test relies on real DB-generated volumeCbm, every route rule walked against the valid fixture. Prettier reformat of its own edits (verified clean). No git drift (HEAD stayed 97cf551).
⚠ **Unit-2 observation → tracked (PRE-EXISTING, untouched, inert):** `impact.classifier.ts:65` self-scope casts `req.entity as FindingScope["type"]`, but `"item"` isn't in the `FindingScope.type` union (`findings.ts`) — only `cargo/package/point/query/leg`(+…). Inert (item changes are Corrective → never gate → scope unused for the decision). Fix = add `"item"` to `FindingScope.type` for coherence (like `"package"` was added in T2) when findings scope types are next touched (Unit 4 / final).

=== ✅ UNIT 2 COMPLETE — routing re-derivation + change-order gating restored. `97cf551..ddf75ec`. → Unit 3 (T9: cargo.service.ts exportXlsx → per-item packing list). **Unit 3 BASE = ddf75ec.** ===
=== 📊 API CORE (T7–T10) now DONE: T7 (item+V2/V4+copy) · Unit1 (query re-model+dgIndicator=T8) · Unit1b+Unit2 (legs+routing=T10). Remaining: T9 export (Unit 3) · then the RIPPLE (Unit 4: rfq/* + ff-portal + config + seed + ~27 legacy specs → full apps/api compile + green e2e) — likely spills to next session per plan. ===

**✅ UNIT 3 COMPLETE — commit `adbc1a4` (parent `ddf75ec`). REVIEW APPROVED (sonnet — headers token-by-token, totals traced, binary-buffer test verified non-tautological).** Re-adds `cargo.service.ts exportXlsx` (dropped in T4) at per-item grain: `Packing List` worksheet, one row per Item (§8.3 columns exact), blank-item row for empty packages, per-PACKAGE totals accumulation (no double-count), package-level DG (`effectiveTags.includes("DG")`), data via `getTree`; `cargo.controller.ts` re-adds `@Post("export")` (@Res xlsx buffer). cargo-export.e2e 4/4 + broader 36/36. Reviewer verified route-collision-free + DTO fields exist.
⚠ **Unit-3 MINORs → final triage (none blocking):** (1) `Package Tags` cell content not asserted (mapping correct by inspection) + item-only-DG-union path untested — add assertions in a coverage pass. (2) totals-row package COUNT sits under the `Package Type` column (brief-sanctioned option; product may prefer a labelled cell). (3) prettier rewrapped 3 pre-existing >100-char lines in cargo.service.ts (whitespace-only) — folds into the branch-wide prettier normalization at final gate. (4) SN = dense 1..N (not item.rowIndex) — matches brief; revisit if gapped SNs wanted post-delete.

=== ✅ UNIT 3 COMPLETE — per-item packing-list export (=T9). `ddf75ec..adbc1a4`. → RIPPLE DECISION POINT (see below). ===

🛑 **RIPPLE SCOPED + DEFERRED (user decision 2026-08-05).** Precise sizing of what's left to make `apps/api` fully compile/e2e-green:
- **REAL broken src = ONLY the Stage-4 quote/portal layer (4 files):** `rfq/manifest.ts` (15 — builds the FF quote manifest from the dropped flat `leg.legCargo.cargoItem.*`, incl. `toKg(weightUnit)` + chargeable-weight), `ff-portal/ff-portal.service.ts` (7), `rfq/leg-context.ts` (2 — `LEG_RFQ_INCLUDE.legCargo`), `rfq/rfq.service.ts` (1). **FALSE POSITIVES (fine, no fix):** `config-data.service.ts` + `seed/reference-seed.ts` use the real `freightDensityFactor` model (substring match); `queries.service.ts:38` + `routing.service.ts:72-88` are the intentional comment / kept `RouteGraph.legCargo{cargoItemId:=packageId}` field-name.
- **25 legacy Stage-4 e2e specs** still build cargo via `prisma.cargoItem`/old API (change-order×5, rfq-distribute/flow/comms/eligibility/expiry/reissue/selection, quote-pricing, charge-config×2, ff-portal, warehouse-attribution, redistribute, create-query-route, queries, query-cargo-model, points-legs-model, prisma-change-log, query-no-response, change-mediator). `apps/api tsc` = **112 errors, ALL in this Stage-4 layer + its specs.**
- **This IS the FF-Portal-v2 work** the project memory flags as PAUSED for coordination (Stage-3 re-model breaks the Stage-4 quote/portal layer; chargeable weight deferred to v2). **User chose: DEFER to that coordinated v2 effort** — do NOT stopgap or re-model it in this run. `apps/api` BOOTS fine (all Stage-3 e2e pass via the isolated configs); only uncalled Stage-4 methods + their specs are red.

════════════════════════════════════════════════════════════════════════
📌 **SESSION END — API CORE (Stage-3 re-model) COMPLETE.**
- **This session shipped 6 feat commits** on `feat/stage-3-cargo-packing-list` over T6-tip `67ff641`: `369e89f` (T7 fixes: V-2/V-4 partial-PATCH + copy rollback) · `9c09116` (T7 Item CRUD) · `d3fc12c` (Unit 1 = T8: query create/read re-model + dgIndicator) · `97cf551` (Unit 1b = T10-legs) · `ddf75ec` (Unit 2 = T10-routing: R1-R9 + change-order gating restored) · `adbc1a4` (Unit 3 = T9 export). Plus a benign concurrent-session docs commit `29294fb`. **Every unit subagent-reviewed → Approved.**
- **Branch integrity (swept):** 18 commits over `main@497e0d2`, linear (main is ancestor); `main` untouched; tree clean (only the other session's untracked `CLAUDE.md`/`docs/README.md`); DB re-model intact on :5433 (Cargo/Item/LegPackage/Package exist; CargoItem/LegCargo gone).
- **End state:** shared ✓ (258/259) · Stage-3 api ✓ green on :5433 (cargo/package/item/query-tree/legs/routing/export all pass via isolated ts-jest) · `apps/api` full typecheck RED **only** in the deferred Stage-4 layer · web intentionally RED (Phase 4).

🔜 **NEXT SESSION resume point (in priority order):**
1. **Coordinate + do the FF-Portal-v2 / Stage-4 ripple** (the 4 src files + 25 specs above) → then `apps/api` typecheck + e2e green. (Blocked on the paused §4.8 coordination — see memory [[project-ff-portal-v2-blocked-on-cargo-remodel]].)
2. **Phase 4 web** (T11–16): data hooks, main table + 2-level expansion, cargo popup, package editor (canonical CBM/MSDS/copies), items mini-table, leg assignment at package grain. **Fold in the tracked read-side fix: `QueryLegDto.assignedCargoIds`→`assignedPackageIds` (shared query.ts:356)** + `ReferenceTagIcons` DG rework.
3. **Phase 5 docs** (T17): align Functional Spec + Technical Design + Handoff.
4. **Final whole-branch review** (opus) + **branch-wide `prettier --write` normalization** (consolidates all the per-unit prettier drift incl. T7's reverted lines) + fix carried Minors → then finish/PR.
🔴 **GO-LIVE GATE (unchanged):** the migration is destructive + auto-deploys to Neon prod on merge — VERIFY Neon prod `CargoItem`/`LegCargo`/`QuoteCargoLine` are empty (or accept the pre-go-live wipe) BEFORE merging. Add to PR body + go-live checklist.

▶ **SESSION CONTINUES — user re-sequenced (2026-08-05): do Phase-4 WEB (T11–16) + DOCS (T17) NOW, before the deferred Stage-4 ripple.** Rationale confirmed: web/docs depend ONLY on the completed Stage-3 API, not the paused Stage-4 quote/portal layer. T11–16 are all **query-wizard** (Step3Cargo + cargo popup + package/item editors + leg-assignment) — NOT the FF portal, so no v2-coordination conflict. Web builds in the same isolated pattern (vitest transpiles per-file; `vite build` skips typecheck) → Stage-3 web components green while Stage-4 web (rfq-workspace/ff-portal) stays typecheck-red until FF-Portal-v2. Web `@svyft/shared` resolves from **dist** → `pnpm --filter @svyft/shared build` before web typecheck/build. Web tests = vitest (`pnpm --filter @svyft/web test -- <path>`), pattern = `renderHook`/`vi.stubGlobal("fetch", …)` + manual `QueryClientProvider` wrapper (see `cargo/useCargo.test.ts`); Radix Select jsdom-flaky → drive hidden native `<select>`.
Current web (all OLD flat-cargo model, Phase-4 red): `steps/cargo/{useCargo.ts,useCargo.test.ts,CargoRowForm.tsx}` · `steps/Step3Cargo.tsx(+test)` · `steps/legs/{CargoAssignmentControl,cargoConflicts,useLegs,usePoints}.*`. Plan web File-structure: replace `CargoRowForm.tsx` with `CargoPopup.tsx`+`PackageEditor.tsx`+`ItemsMiniTable.tsx`; add `usePackages.ts`+`useItems.ts`. Endpoints (from the done API): cargo `POST/PATCH/DELETE /queries/:id/cargo[/:cid]` + `POST …/cargo/export`; package `POST …/cargo/:cid/packages`,`PATCH/DELETE …/packages/:pid`,`POST …/packages/:pid/msds`,`POST …/packages/:pid/copies`; item `POST …/packages/:pid/items`,`PATCH/DELETE …/items/:iid`.
- [x] T11 web: data hooks (useCargo grouping + usePackages + useItems) ✅ `29affd6` — Unit W1, REVIEW APPROVED (sonnet, cross-checked every endpoint vs live controllers + DTO fields vs shared; all 11 mutators bust `["query",queryId]`). vitest 13/13, 3 hook files tsc-clean. `uploadMsds` moved cargo→package; fixtures typed `:CargoDto` etc. so drift = compile error. **Minor (track): `usePackages.ts` re-interpolates the base path 5× (useItems factors it) — DRY nit.** T12 BASE = `29affd6`.
- [x] T12 web: main table + 2-level expansion (Step3Cargo) ✅ `9e8c128` + fix `8547894` — Unit W2, REVIEW APPROVED (sonnet, verified full source: colSpans, fromCanonical wiring, DG rework, inert Add/Edit stub). Nested cargo→package→item table reading `detail.cargos`; ReferenceTagIcons DG-as-tag (added DG to TAG_ICON, dropped `isDangerous`); shared `QueryDetail.cargo`→`cargos`. **Important fix applied+committed (`8547894`): added MM/GM conversion test (verified genuinely fails if `fromCanonical*` were identity) + `cargoLabel` falls back to `.label`.** vitest 9/9. **Minors (track): report's tsc blast-radius count off (doc only); Add/Edit are placeholder-Dialog stubs → T13 wires CargoPopup.**
  - 🔧 **LIVE WEB RIPPLE surfaced by the mandatory shared rebuild (NOT a T12 defect; pre-existing since T2, was latent behind stale web dist): `QueryWizardPage.tsx`'s `toCargoForValidation` builds the OLD cargo shape for the T2-changed `collectCreateFindings(…, PackageForValidation[])` → "Create Query" THROWS at runtime + ~11 web tests fail.** Also `routeGraph.ts`/`LegEditor.tsx` read dropped `CargoDto` fields (`productName`/`isDangerous`/`grossWt`) — same flat→nested ripple. **→ FIX as Unit W2b (next): re-point the wizard's create-validation to build `PackageForValidation[]` from `detail.cargos` (+ any routeGraph flat-cargo reads).** Implementer spawned chip `task_918c2a42`.
- [x] T13–T15 web: cargo entry popup (Unit W3) ✅ `ecacad5` — REVIEW APPROVED (sonnet, re-ran 26/26, traced client CBM math + no-orphan save-flow + the aria/remount fixes). CargoPopup (cargo fields + unit selectors; persists cargo→packages→items with real ids) + PackageEditor (packageType dropdown, client-side CBM preview `cbmFromCanonical(toCanonicalDim)`, entry-unit values sent as-is/server converts, MSDS shown only when `effectiveTags` DG, add-N-copies) + ItemsMiniTable (V-4 via schema resolver, item-DG flips package DG) + shared `cargo-form-fields.tsx`; Step3Cargo Add/Edit wired; **CargoRowForm deleted (688 lines)**. new components + Step3Cargo tsc-clean; eslint/prettier clean. **5 MINORs → final triage:** (1) dup "Save" a11y name (cargo+package both "Save" in one Dialog → rename package one); (2) no POST-body test for entry-unit dims (code verified correct, but untested — same failure-mode as T12's fixed gap); (3) PackageAddForm/PackageEditForm ~250-line JSX dup (RHF generic friction, disclosed); (4) weak assertion in bonus Edit-prefill test (CM/KG fixture); (5) copies count no client min/max hint. **T16 BASE = `ecacad5`.**
- [x] T16 web: leg assignment at package grain (Unit W4) ✅ `45ef5b4` — REVIEW APPROVED (sonnet, independently verified shapeQuery emits assignedPackageIds, re-ran 31/31, conflict algo byte-identical, LegEditor scope minimal). Tick-list = packages grouped under cargo PO/Ref; `computeCargoConflicts`→`computePackageConflicts` (package-id map); shared `QueryLegDto.assignedCargoIds`→`assignedPackageIds`; minimal LegEditor integration; useLegs payload-key fix; fixtures re-grained. cargoConflicts 6/6, CargoAssignmentControl 12/12, useLegs 5/5, LegEditor 13/13; shared 260/260. **Minor (track): LegEditor "Assigned Cargo" label → "Assigned Packages" (cosmetic).**
  - **DEFERRED Stage-3-web ripple (NOT done — track):** `routeGraph.ts`/`RouteDiagram.tsx` (client route-diagram reads dropped flat CargoDto fields — needs cargos→packages re-model) + `QueryWizardPage.tsx` Create-Query `toCargoForValidation` (builds old shape for `collectCreateFindings` → throws; ~11 web tests). These are Stage-3 (not FF-portal-v2) → fix in a Stage-3-web-ripple pass. `rfq-workspace/*` (PreviewRfqDialog etc.) = Stage-4/FF-portal-v2 (deferred).

════════════════════════════════════════════════════════════════════════
📌 **WEB PHASE (T11–16) + DOCS (T17) COMPLETE** — all subagent-reviewed → Approved. Session-2 commits over API-core tip `adbc1a4`: `29affd6` T11 hooks · `9e8c128`+`8547894` T12 table+fix · `23f2205` T17 docs · `ecacad5` W3 entry-popup(T13-15) · `45ef5b4` T16 legs. Branch tip `45ef5b4`.
**Stage-3 feature is now end-to-end usable in the UI:** view the cargo tree, enter cargo→packages→items via the popup (CBM/MSDS/copies/V-4/DG), assign packages to legs. Web builds isolated-green on the Stage-3 surface; typecheck-red remains ONLY in the two deferred ripples below.
🔜 **REMAINING (next):**
1. ✅ **DONE — Stage-3 web ripple** (`bbbe348` W5 code + `c7f99f6` W5b fixtures, REVIEW APPROVED sonnet): `routeGraph.ts` re-modelled to mirror the API `buildGraph` (one RouteCargo per Package) + `RouteDiagram` badge + `QueryWizardPage` Create-Query `PackageForValidation[]` (was throwing) + 3 Stage-3 test-file fixtures (LegsStep/QueryWizard.e2e/WizardShell). **Stage-3 web is now FULLY GREEN (typecheck + tests);** full web suite 428/437 — the ONLY red left is `rfq-workspace/*` (Stage-4/paused) + 1 pre-existing date-rollover flake (Step1Client). Reviewer traced client/server route-graph parity + PackageForValidation/F6 match. Minors (cosmetic, track): `CARGO_ID_Q9` now holds a package id (rename); "1 packages" plural (brief-specified).
2. **Stage-4 ripple = FF-Portal-v2 (PAUSED, coordinated):** api `rfq/manifest.ts`+`ff-portal.service`+`rfq/leg-context`+`rfq.service` + web `rfq-workspace/*` + ~25 Stage-4 e2e specs.
3. **Final whole-branch review (opus) + branch-wide `prettier --write` normalization + carried Minors** (T6 :cid, T7 assertApplied helper, Unit-2 FindingScope "item", T12 tsc-count doc, W3 ×5, W4 label, W5 ×2, doc §8.6 contradiction) → finish/PR.
**Branch tip `c7f99f6`; 25 commits over `main@497e0d2`, linear.**
🔴 **GO-LIVE GATE (unchanged): verify Neon prod CargoItem/LegCargo/QuoteCargoLine empty before merge; branch WIP until ripple+final-review done.**
════════════════════════════════════════════════════════════════════════
- [x] T17 docs: align Functional Spec + Technical Design ✅ `23f2205` (Handoff left to concurrent session; broader cargo-row language deferred — §8.6 contradiction is top doc follow-up).

📋 **Carried MINORs for the final-review triage** (none block; consolidated): T6 `copy():cid` not cross-checked (harmless) · T7 ConflictException-boilerplate ×7 (extract `assertApplied` helper) · Unit-1b/2 `QueryLegDto.assignedCargoIds` stale + `package.service.ts:43` stale comment · Unit-2 `FindingScope.type` missing `"item"` (add for coherence) · Unit-3 Package-Tags/DG-via-item-tag coverage + totals-count-under-PackageType layout · branch-wide prettier normalization.
════════════════════════════════════════════════════════════════════════

════════════════════════════════════════════════════════════════════════
# SDD Run 2 — FF Portal v2 (§4.8) + Cargo→Package ripple
Plan: `docs/plans/stage-4/Stage 4 - FF Portal v2 - Implementation Plan.md` (25 tasks, 6 units)
Design: `docs/Stage 4 - FF Portal v2 - Design.md`
Branch: `feat/stage-3-cargo-packing-list`. Run-2 BASE (feat tip before Task 1) = **4826ff5** (plan commit; parent 89a6c06 design).
Models: haiku = transcription (full code in plan), sonnet = integration + reviewers, opus = final whole-branch review.
**SCOPE THIS PASS: Unit 1 ONLY (Tasks 1–3) — user directive.** Stop after Task 3 (nest build green); do NOT start Unit 2 without the user.
Protocols (verbatim from plan Global Constraints): controller commits checkout-first + verify parent; implementers NEVER touch git; api tasks run e2e via an ISOLATED-TRANSPILE jest config (scratchpad copy of `apps/api/test/jest-e2e.json` + ts-jest `isolatedModules:true`, NOT committed); `pnpm --filter @svyft/shared build` after every shared edit; hand-authored SQL + `migrate deploy` never `migrate dev`; DB :5433 + `set -a; . apps/api/.env; set +a`; leave `CLAUDE.md`/`docs/README.md` untracked. 🔴 destructive-migration go-live gate — no merge until whole branch green + opus-reviewed + user-approved.

## Unit 1 — Ripple unblock (migration-free grain re-point → `apps/api build` green)
- [x] Task 1 shared `ManifestSnapshotCargo` → per-package (BASE 4826ff5) ✅ 359920e
- [x] Task 2 `leg-context` include + `manifest` builder → package grain (BASE 359920e) ✅ 7d0f86a
- [x] Task 3 `rfq.service` eligibility + `ff-portal.service` re-point (grain only; density intact) → `nest build` green (BASE 7d0f86a) ✅ e8d7fc6

### Ledger
(one line appended per task as each review comes back clean)
**Task 1: complete** (commits `4826ff5..359920e`, sonnet review clean — spec ✅, no Critical/Important). `ManifestSnapshotCargo` → per-package (packageId/packageNo/packageType/packageCount/canonical dims cm + gross/net kg + volumeCbm + `tags: ReferenceTag[]`); flat CargoItem fields gone; `import type { ReferenceTag } from "./cargo"` (no cycle — cargo.ts imports only zod). `rfq.test.ts` pins the shape + a genuine `@ts-expect-error` negative assertion. shared build+test+typecheck green. api consumers intentionally red until Task 2/3.
⚠ **Task-1 MINOR → final triage:** `rfq.test.ts` negative assertion exercises 3 of 6 removed fields (isDangerous/qty/productName; not cargoItemId/poReference/hsCode) — plan-mandated, low-risk (interface fully replaced, not extended). Strengthen if this interface is ever additively edited.
**Task 2: complete** (commits `359920e..7d0f86a`, sonnet review clean — spec ✅, no Critical/Important). `leg-context.ts` `LEG_RFQ_INCLUDE.legPackages` include + `hasDg` via `effectiveTags(...).includes("DG")`; `manifest.ts` builds `cargo[]` per package from `leg.legPackages.package.*` (canonical cm/kg straight from DB, `toKg` GONE, `tags`=`effectiveTags`). New `rfq-manifest.e2e-spec.ts` builds real Cargo→2 Packages(1 DG item)→leg→`LegPackage` + drives the REAL `loadLegForRfq`→`buildManifestSnapshot` chain (same order as `RfqService.performDistribution`). **DEVIATION (reviewer-verified acceptable):** not routed through live `POST /distribute` because `rfq.service.ts:267` (`leg.legCargo.length`) still 500s — that's Task 3's fix; full HTTP distribute path gets covered by Task 3's e2e + Unit-5 spec rewrites. RED→GREEN via isolated config; legs/item regression green.
⚠ **Task-2 MINORs → final triage:** (1) DG assert checks `some(...DG)` but not that the non-DG package EXCLUDES DG (per-package grain not airtight; can't leak — items are relation-scoped). (2) neither fixture package sets `netWt` → manifest.ts `netWt?…:null` populated-branch untested (1-line mirror of grossWt).
**Task 3: complete** (commits `7d0f86a..e8d7fc6`, sonnet review clean — spec ✅, no Critical/Important). `rfq.service.ts:267` F1 `leg.legCargo.length`→`leg.legPackages.length`; `ff-portal.service.ts` grain-only re-point — resolveScope seededDensity `cargoItemId: c.packageId`; submit draft.cargo (`cargoItemId: c.packageId`, `isDangerous: c.tags.includes("DG")`, freightDensity looked up by packageId); `quoteCargoLine.createMany` writes `packageId: c.cargoItemId`. **Density model KEPT intact** (freightDensity/chargeableWeightT/computeChargeableWeight) — Unit-2 removes it; NO shared edit (DTO field names stay). New `ff-portal-grain.e2e-spec.ts` distributes over the REAL HTTP path → GET portal → submit → asserts one QuoteCargoLine per package with packageId (closes Task-2's deferred HTTP-path coverage; clever non-vacuous Q5 proof that submit re-derives isDangerous from manifest tags). **Controller INDEPENDENTLY re-ran `pnpm --filter @svyft/api build` → exit 0 (nest build GREEN).**
⚠ **Task-3 MINOR → final triage (largely MOOT post-Unit-2):** the Q5/DG test only asserts the positive "≥1 DG" path, no all-non-DG negative fixture → doesn't fully pin per-row derivation (mirrors F5's proven convention; the whole density/Q5 path is rewritten in Unit 2/5 anyway).

════════════════════════════════════════════════════════════════════════
✅ **UNIT 1 COMPLETE — Cargo→Package ripple unblock. `nest build` GREEN.** 3 feat commits over plan tip `4826ff5`: `359920e` (T1 shared manifest type) · `7d0f86a` (T2 leg-context+manifest builder) · `e8d7fc6` (T3 rfq.service+ff-portal.service). Every task subagent-reviewed → Approved. Migration-free (QuoteCargoLine.packageId already existed; manifestSnapshot is Json). Density model still present (Unit-2 removes it). `pnpm --filter @svyft/api build` verified green by the controller. api full `typecheck` still RED only in the ~25 legacy Stage-4 specs (Unit 5) + Unit-2/3 not yet built. Branch NOT deployed (🔴 destructive-migration go-live gate unchanged).
🔜 **RESUME (awaiting user go): Unit 2 (Tasks 4–8) — additive migration + engine v2.** Run-2 BASE for Unit 2 Task 4 = `e8d7fc6`. Isolated jest config lives at scratchpad `jest-e2e.isolated.json` (proven; NOT committed).
════════════════════════════════════════════════════════════════════════

## Unit 2 — Model/engine v2 (additive migration + shared engine). ▶ IN PROGRESS (user go 2026-08-06)
**DECOMPOSITION CHANGE (controller):** plan Tasks 5/6/7 are type-coupled — changing `QuoteDraft` (T5) breaks the still-old `computeQuoteTotals`/`validateQuote` in the same `quote-engine.ts` until T6/T7 land, so shared can't typecheck-green individually. **Merged 5+6+7 → one atomic "shared quote-layer v2" task** (gate: full shared build+typecheck+test green). Task 8 (two-gate + seed) stays separate (`resolveChargeConfig` gets a defaulted 4th param → api caller stays compatible).
**BUILD REALITY:** applying the migration (Task 4) re-breaks `nest build` (Unit-1 `ff-portal.service.ts` still writes the dropped `freightDensity`/`chargeableWeightT` cols) — EXPECTED; restored at Unit 3 Task 9. Unit-2 api gate = isolated-transpile e2e, NOT nest build.
**DB pre-migration snapshot (:5433):** Quote/QuoteCargoLine/Trucking/ChargeLine/Warehouse/Transit/Package/Cargo = 0 rows; ChargeLineDefinition = 49. Column drops affect zero rows (safe).
- [x] Task 4 Prisma migration — kg rename, dual-rate models/enums, calc cols (BASE e8d7fc6) ✅ b6fe905. Migration folder `20260806000000_ff_portal_v2`.
- [x] Task 5′ (=plan T5+T6+T7 merged) shared quote-layer v2 — draft shapes/enums + engine per-variant totals + heavy-weight calc + submit-gate v2 (BASE b6fe905) ✅ 1ac8f78.
- [x] Task 6′ (=plan T8) tag two-gate resolver + catalogue seed (FSC/Peak/HEAVY_WEIGHT_CALC) **+ deactivate SEA_MAIN_FREIGHT + Air sortOrder fix** (BASE 1ac8f78) ✅ 52603ee + fix 5b87118.

### Ledger (Run 2 · Unit 2)
**Task 4: complete** (commit `b6fe905`, parent `7b05ab6`; sonnet review clean — spec ✅, zero findings). Migration `20260806000000_ff_portal_v2` applied to :5433 via `migrate deploy` (no destructive prompts; guardrails held). QuoteCargoLine drop freightDensity/chargeableWeightT → add `chargedWeightKg Decimal(12,3)`; 5 new enums + `HEAVY_WEIGHT_CALC`; TruckingCharge rateVariant(default DEDICATED)/tonnage?; new SeaFreightRate(+Quote back-rel); ChargeLine calc cols+billOfLadingType; WarehouseStagingLine cfsCode/side; TransitPlan 9 mode cols. **Controller DB-verified: columns/enums/table/migration-row all present; row counts 0 (ChargeLineDefinition=49) → zero data loss.** schema↔migration.sql byte-consistent; safe NOT-NULL backfill; ADD VALUE isolated. `nest build` now RED (Unit-1 ff-portal.service writes dropped cols) → restored Task 9. No Minors.
**Task 5′: complete** (commit `1ac8f78`, parent `b6fe905`; sonnet review — Approved). Merged plan T5+T6+T7 in `quote.ts`/`charge-config.ts`/`quote-engine.ts`(+tests) + `ff-portal.ts` `quoteDraftSchema` sync (+`ff-portal.test.ts` fixture-only). Engine math letter-perfect (Road group-by-rateVariant / Sea per-seaRate / Air single; sharedSubtotal=Σcharges+Σwarehouse; chargeableWeightKg=ΣchargedWeightKg; no double-count in engine); `computeHeavyWeightAmount`; `validateQuote` v2 6-rule gate; `computeChargeableWeight`+old QuoteTotals deleted; old tests genuinely rewritten (retired Q2/Q5/Q6 = documented redesign). **Controller INDEPENDENTLY re-ran shared build+typecheck+test → all green (271).** Implementer correctly STOPPED on the `ff-portal.ts` TS2352 (seaRates required breaks the `as z.ZodType<QuoteDraft>` cast) instead of weakening seaRates → controller folded the `quoteDraftSchema` sync into this task (the Zod schema IS the runtime mirror of the type).
🔴 **Task-5′ review IMPORTANT (cross-task; NOT a T5′ defect — code correct) → FOLDED INTO Task 6′:** `reference-seed.ts:56` still seeds `SEA_MAIN_FREIGHT` as active CORE/PLAIN. Design §5.2/§8 makes sea freight the structured `seaRates[]` dual-rate that REPLACES the flat line — so once Unit 3 wires the portal, a Sea leg would price sea freight TWICE (draft.charges Q_PRICED + seaRates Q_RATE; computeQuoteTotals sums both). Fix in Task 6′: seed def `SEA_MAIN_FREIGHT.isActive=false` (fresh DBs) **+ a data migration `UPDATE "ChargeLineDefinition" SET "isActive"=false WHERE "key"='SEA_MAIN_FREIGHT'`** (seed is create-only → can't update the live :5433/prod row) + a test asserting it's absent from a resolved Sea snapshot. (Road/Air unaffected: ROAD_CORE_TRUCKING is TRUCKING inputType; Air is single-rate.)
⚠ **Task-5′ MINOR → triage:** dual-rate gate tests cover "row present, amount null" but not truly-empty `trucking:[]`/`seaRates:[]` (code correct — `.some([])`=false).
**Task 6′: complete** (commits `52603ee` + fix `5b87118`, parent chain `1ac8f78`; sonnet reviewed twice — Approved). `resolveChargeConfig` v2: 4th param `packageTags` (defaulted → api caller charge-config.snapshot.ts still compiles, Unit-3 Task 10 passes tags); TAG_DRIVEN activates iff selected AND tagKey∈packageTags; `lines` include PLAIN+HEAVY_WEIGHT_CALC. Seed: +AIR_MAIN_FSC/+AIR_MAIN_PEAK_SEASON, AIR_MAIN_HEAVY_WEIGHT→HEAVY_WEIGHT_CALC, SEA_MAIN_FREIGHT→isActive:false. **THREE data migrations** (create-only seed can't update existing rows): `20260806010000_retire_sea_main_freight`, `20260806020000_backfill_air_heavy_weight_calc` (implementer-found: same create-only issue for the inputType flip), `20260806030000_air_surcharge_sortorder`. **Review IMPORTANT (sortOrder) — FIXED:** plan used `sortOrder: 9.1/9.2` but the col is Int → truncated to 9, tying FSC/Peak/HeavyWeight; fix renumbers FSC=10/Peak=11 + downstream Air +2 (→12..20) + a data migration + a distinct-ascending regression test. **Controller DB-verified: Air catalogue 6..20 no ties in any (mode,zone); count 51.** shared build+typecheck+test green (274). Plan doc §T8 corrected (SEA retirement step + Int-sortOrder note).
⚠ **Task-6′ MINOR → triage:** the belt-and-suspenders `toEqual([9,10,11])` in the sortOrder test duplicates the three ordering asserts above it (harmless).

════════════════════════════════════════════════════════════════════════
✅ **UNIT 2 COMPLETE — model/engine v2.** 4 feat commits over Unit-1 tip `7b05ab6`: `b6fe905` (T4 migration) · `1ac8f78` (T5′ shared quote-layer v2) · `52603ee` (T6′ two-gate+seed) · `5b87118` (T6′ sortOrder fix). Every task subagent-reviewed → Approved. shared build+typecheck+test GREEN (274). DB :5433 migrated (5 migrations total on branch: cargo-remodel + ff_portal_v2 + retire_sea + backfill_air_calc + air_sortorder); ChargeLineDefinition=51, no sortOrder ties. **`nest build` is RED (expected — Unit-1 ff-portal.service still writes dropped density cols; restored in Unit 3 Task 9).** Branch NOT deployed (🔴 go-live gate).
## Unit 3 — API v2 wiring. ▶ IN PROGRESS (user go 2026-08-06)
**GATE REALITY:** Task 9 rewrites `ff-portal.service.ts` off the dropped density cols → **RESTORES `pnpm --filter @svyft/api build` GREEN** (the Unit-2 red clears). Task 10 keeps it green.
**buildChargeConfigSnapshot has 2 callers** (Task 10 signature change must update BOTH): `rfq.service.ts:374` (distribute) + `changes/change-order.strategy.ts:155` (SB6 re-freeze) — each must build + pass the manifest's per-package `cargo` tags. nest build gate catches a missed caller.
- [x] Task 9 ff-portal.service v2 + ff-portal.ts DTO (drop seededDensity) — chargedWeightKg, dual-rate trucking+SeaFreightRate, heavy-weight calc, per-variant grandTotal → `nest build` GREEN (BASE d993a68) ✅ 7624392 + fixes c1b0503.
- [x] Task 10 distribute freeze two-gate — charge-config.snapshot takes manifestCargo → packageTags; update both callers (rfq.service + change-order.strategy) ✅ 40e480e.

### Ledger (Run 2 · Unit 3)
**Task 9: complete** (commits `7624392` portal v2+engine + `c1b0503` 4 gate/materialize fixes; parent `d993a68`; sonnet reviewed TWICE — Approved). **RESTORES `nest build` GREEN** (controller re-verified). ff-portal.service v2: resolveScope drops density block (+ff-portal.module drops now-dead ConfigDataModule), seeds Charged Wt null; submit materializes `QuoteCargoLine.chargedWeightKg`, dual `TruckingCharge`(rateVariant/tonnage)+`SeaFreightRate`, HEAVY_WEIGHT_CALC via chargeAmount, warehouse cfsCode/side, mode-specific transit, `grandTotal`=max variant. Shared DTO drops `FfPortalSeededDensity`/`seededDensity`. **Engine fix folded in:** `computeQuoteTotals` now folds HEAVY_WEIGHT_CALC derived amounts via `effectiveChargeAmount` (was under-counting grandTotal → feeds Stage-5). **Review + implementer found 4 force-unwrap crash/corruption paths → all FIXED (c1b0503):** (1) Q_WEIGHT (chargedWeightKg mandatory per package; was 400 not 422); (2) Q_CUSTOM_AMOUNT (custom [+Add] needs amount not just remark); (3) filter unpriced trucking/seaRates before materialize (gate allows ≥1 variant); (4) `TransitPlan.departureDate/arrivalDate`→nullable (migration `20260806040000_transit_dates_nullable`) + null-guard (v2 gate dropped departure/arrival for guaranteedTransitDays; `new Date(null)` wrote 1970 to every quote). shared 279/279; ff-portal-grain e2e 5/5; nest build 0 errors; TransitPlan cols verified nullable on :5433 (6 branch migrations now).
⚠ **Task-9 MINORs → triage:** (1) engine ROAD branch: a variant with only a blank (null-amount) row resolves rateAmount→0 not null, so Unit-4 would show `$0.00` not `–` (SEA preserves null; asymmetry) — fix in Unit-4 engine polish. (2) seaRates unpriced-filter has NO e2e (trucking tested; SEA correct by inspection — no SEA-mode submit spec exists) — add in Unit 5. (3) stale addendum comment in ff-portal-grain spec (doc-only).
**Task 10: complete** (commit `40e480e`, parent `c1b0503`; sonnet review — Approved, thorough). `buildChargeConfigSnapshot(prisma, leg, manifestCargo)` computes `packageTags`=union of `manifestCargo[].tags` (=effectiveTags, item∪package — reviewer confirmed a DG item tag reaches it) → `resolveChargeConfig(…, packageTags)`. BOTH callers pass their fresh same-leg manifest: `rfq.service:374` `snapshot.cargo`, `change-order.strategy:155` `snap.cargo`. New `tag-two-gate.e2e-spec.ts` (DG via Item.tags, Package tags empty → forces the effectiveTags path): Exec selects AIR_TAG_DG+AIR_TAG_FRAGILE, packages carry only DG → frozen snapshot has AIR_TAG_DG, NOT AIR_TAG_FRAGILE. **Note:** pre-Task-10 the 3-arg callers defaulted packageTags=[] → ALL tag-driven lines were disabled since Task 6′; this re-enables them correctly. nest build GREEN (both callers). Controller re-verified build.
⚠ **Task-10 MINORs → triage:** (1) change-order.strategy re-freeze two-gate has only type-level coverage (natural host `change-order-cascade.e2e-spec` is Unit-5-red) → add "two-gate re-resolves on re-freeze" assertion when Unit 5 rewrites it. (2) e2e covers "selected-not-carried→excluded"; the complementary "carried-not-selected→excluded" half is unit-tested in charge-config.test.ts (fine).

════════════════════════════════════════════════════════════════════════
✅ **UNIT 3 COMPLETE — API v2 wiring. `nest build` GREEN again.** 3 feat commits over Unit-2 tip `d993a68`: `7624392` (T9 portal v2 + engine calc-total fix) · `c1b0503` (T9 4 gate/materialize fixes) · `40e480e` (T10 two-gate freeze). Every task subagent-reviewed → Approved (T9 twice). shared build+typecheck+test GREEN (279); `pnpm --filter @svyft/api build` GREEN (controller-verified). 6 branch migrations on :5433. The portal now serves + submits the full v2 model (chargedWeightKg, dual-rate, calc, two-gate); distribute freezes per-package tags + tag-gated lines.
🔴 **KEY GATE STILL RED (expected): full `pnpm --filter @svyft/api typecheck` + the ~25 legacy Stage-4 e2e specs** — those still use the old cargo model (Unit 5). And `pnpm run ci` is RED until Unit 5. `nest build` (src only) is green; api e2e that touch the v2 paths pass via the isolated config.
🔜 **RESUME (awaiting user go): Unit 4 (Tasks 11–18) — WEB** (exec rfq-workspace re-point + FF portal v5: package list/Charged-Wt, dual-rate Road+Sea, Air FSC/Peak+calculator, mode transit, route SVG, PDF/preview). Then Unit 5 (rewrite ~25 e2e specs → full ci green) + Unit 6 (final gate + opus review + go-live-gated PR). Run-2 BASE for Unit 4 Task 11 = `40e480e`.
════════════════════════════════════════════════════════════════════════
════════════════════════════════════════════════════════════════════════
════════════════════════════════════════════════════════════════════════
════════════════════════════════════════════════════════════════════════
